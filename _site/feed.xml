<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="zh-Hans"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://lixiaofei2yy.website/feed.xml" rel="self" type="application/atom+xml" /><link href="https://lixiaofei2yy.website/" rel="alternate" type="text/html" hreflang="zh-Hans" /><updated>2026-09-11T19:36:03+08:00</updated><id>https://lixiaofei2yy.website/feed.xml</id><title type="html">凡人炼丹传</title><subtitle>欢迎各位看官光临本小站，希望共同学习进步哈！
</subtitle><author><name>李小肥</name><email>yeyansiwangtt@gmail.com</email></author><entry><title type="html">Mac真的适合跑本地Agent吗</title><link href="https://lixiaofei2yy.website/2026/09/11/Mac%E9%80%82%E5%90%88%E8%B7%91%E6%9C%AC%E5%9C%B0Agent.html" rel="alternate" type="text/html" title="Mac真的适合跑本地Agent吗" /><published>2026-09-11T08:00:00+08:00</published><updated>2026-09-11T08:00:00+08:00</updated><id>https://lixiaofei2yy.website/2026/09/11/Mac%E9%80%82%E5%90%88%E8%B7%91%E6%9C%AC%E5%9C%B0Agent</id><content type="html" xml:base="https://lixiaofei2yy.website/2026/09/11/Mac%E9%80%82%E5%90%88%E8%B7%91%E6%9C%AC%E5%9C%B0Agent.html"><![CDATA[<h2 id="一本地-agent-为什么和普通聊天完全不是一回事">一、本地 Agent 为什么和普通聊天完全不是一回事？</h2>
<p>很多人第一次在 Mac 上跑本地大模型，都会有一种很强烈的感受：<strong>安静、顺滑、吐字快，体验比想象中好很多</strong>。于是一个问题自然就来了：<strong>Mac 真的适合跑本地 Agent 吗？</strong></p>

<p>我的结论很直接：<strong>适合，但要分场景。</strong></p>

<p>如果你主要是和模型做普通对话，Mac 的体验往往非常好；但如果你开始跑 Coding Agent、工具调用、多 Agent 协作这类任务，你会发现瓶颈并不总在“吐字速度”，而常常出在 <strong>Prefill（预填充）</strong> 这一段。也就是说，<strong>Mac 很擅长做本地推理链路的后半程，却不一定擅长前半程。</strong></p>

<p>这篇文章就想把这个问题讲透：</p>

<ul>
  <li>为什么普通聊天和本地 Agent 的体验差异会这么大？</li>
  <li>Mac 在本地推理这件事上，真正的优势和短板分别是什么？</li>
  <li>如果你想把本地 Agent 跑得更舒服，Mac 最适合扮演什么角色？</li>
</ul>

<h3 id="11-kv-cache-到底解决了什么问题">1.1 KV Cache 到底解决了什么问题？</h3>

<p>可以把大模型理解成一个<strong>边看上下文、边往后续写</strong>的系统。</p>

<p>如果它每生成一个新 Token，都必须把从头到尾的上下文重新完整读一遍，那推理速度会慢得无法接受。为了解决这个问题，推理系统会把之前已经算过的注意力结果缓存下来，这就是 <strong>KV Cache（Key-Value Cache）</strong>。</p>

<p>KV Cache 的核心作用可以概括成两点：</p>

<ul>
  <li><strong>避免重复计算</strong>：历史 Token 对应的 Key 和 Value 会被缓存起来。</li>
  <li><strong>只计算最新内容</strong>：生成下一个 Token 时，模型只需要处理新增部分，再结合已有缓存继续往下推理。</li>
</ul>

<p>你可以把它理解成：模型不是每次都从头“重读全文”，而是在<strong>不断复用前面已经做好的笔记</strong>。</p>

<blockquote>
  <p>在 <code class="language-plaintext highlighter-rouge">ollama</code> 和 <code class="language-plaintext highlighter-rouge">GGUF</code>（<code class="language-plaintext highlighter-rouge">llama.cpp</code>）相关模型或量化配置里，后缀中如果出现 <code class="language-plaintext highlighter-rouge">KV</code>、<code class="language-plaintext highlighter-rouge">K_V</code> 等标识，通常是在表达 <strong>KV Cache 的量化或优化配置</strong>。在长上下文场景（例如 32k、128k）中，KV Cache 会占用大量显存/内存，因此对 KV Cache 做量化，往往可以显著降低上下文缓存成本。</p>
</blockquote>

<h3 id="12-llm-推理其实分成两个阶段">1.2 LLM 推理其实分成两个阶段</h3>

<p>很多人把“模型推理速度”理解成一个单一指标，但实际上，大模型推理通常可以拆成两个体验非常不同的阶段：</p>

<h4 id="预填充阶段prefill--context-phase">预填充阶段（Prefill / Context Phase）</h4>

<p>这是模型<strong>一次性读完整段 Prompt</strong> 的过程。</p>

<p>在这个阶段，模型要把整段输入上下文都处理一遍，并建立后续解码所需的 KV Cache。这个过程更吃 <strong>算力</strong>，尤其当上下文非常长时，等待时间会迅速上升。</p>

<h4 id="解码阶段decode-phase">解码阶段（Decode Phase）</h4>

<p>这是模型<strong>一个 Token 一个 Token 往外吐字</strong> 的过程。</p>

<p>这个阶段通常更偏向 <strong>带宽主导</strong>：每生成一个新 Token，系统都要反复读取模型权重与已有缓存。计算本身未必极重，但对内存带宽和整体数据搬运效率非常敏感。</p>

<p>所以你会看到一个很典型的现象：</p>

<ul>
  <li>有些设备<strong>吐字很快</strong>，看起来聊天体验很好；</li>
  <li>但只要 Prompt 一长，或者上下文一复杂，<strong>按下发送后的第一段等待时间会明显变长</strong>。</li>
</ul>

<p>这其实就是 <strong>Prefill 和 Decode 侧重点不同</strong> 导致的体验分裂。</p>

<h3 id="13-为什么本地-agent-对-prefill-更敏感">1.3 为什么本地 Agent 对 Prefill 更敏感？</h3>

<p>普通聊天通常是：<strong>输入短，输出长</strong>。</p>

<p>而很多本地 Agent 场景刚好反过来：<strong>输入极长，输出极短</strong>。</p>

<p>例如在 Coding Agent 或多 Agent 系统中，一次请求里可能包含：</p>

<ul>
  <li>System Prompt</li>
  <li>工具定义</li>
  <li>历史对话</li>
  <li>文件片段</li>
  <li>搜索结果</li>
  <li>工具返回值</li>
  <li>子代理上下文</li>
</ul>

<p>最终输入长度可能非常大，但模型输出往往只是一小段计划、一个工具调用，或者几百字结论。此时，时间主要耗在 <strong>Prefill</strong>，而不是 Decode。也正因为如此，<strong>本地 Agent 的体感，并不能简单等同于“吐字速度快不快”。</strong></p>

<p>更麻烦的是，KV Cache 不是永远都能复用。它能复用的前提是：<strong>前缀 Token 序列及其对应的 Attention Mask 必须保持一致。</strong> 一旦这个条件被破坏，之前缓存的结果就失效，系统必须重新做一遍 Prefill。</p>

<p>常见的缓存失效场景包括：</p>

<ul>
  <li><strong>插入工具回传结果</strong>：例如插入 JSON、搜索结果、执行日志，这会改变上下文中间部分，导致后续缓存失效。</li>
  <li><strong>新建子代理</strong>：子代理往往拥有独立的 System Prompt 和任务结构，无法直接复用主代理的缓存。</li>
  <li><strong>重启程序</strong>：KV Cache 通常驻留在显存或运行时内存中，程序重启后自然也就没法继承。</li>
</ul>

<p>所以一旦进入 Agent 场景，很多人会第一次意识到：<strong>本地推理的真正痛点，往往不是模型“吐得慢”，而是“想得太久”。</strong></p>

<h2 id="二mac-在本地大模型上的真实位置">二、Mac 在本地大模型上的真实位置</h2>

<h3 id="21-mac-在-llm-推理上的优劣势">2.1 Mac 在 LLM 推理上的优劣势</h3>

<p>如果从真实使用体验出发，我会把 Mac 的优劣势总结成下面这张表：</p>

<table>
  <thead>
    <tr>
      <th>规格类别</th>
      <th>决定什么</th>
      <th>Mac 的表现</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>内存（RAM）</strong></td>
      <td>能否装下更大模型</td>
      <td><strong>优势很大</strong>：统一内存架构让 CPU / GPU 共用同一块内存池，适合承载更大模型</td>
    </tr>
    <tr>
      <td><strong>内存带宽</strong></td>
      <td>吐字速度有多快</td>
      <td><strong>中上优势</strong>：高阶芯片带宽足够高，日常聊天与常规本地推理体验通常很好</td>
    </tr>
    <tr>
      <td><strong>GPU 算力</strong></td>
      <td>按下发送后的等待时间</td>
      <td><strong>相对弱项</strong>：在 Prefill 阶段往往不占优，长上下文和 Agent 类任务更容易暴露这一点</td>
    </tr>
    <tr>
      <td><strong>能效与散热</strong></td>
      <td>噪音、功耗、稳定性</td>
      <td><strong>优势非常明显</strong>：安静、省电、桌面友好，长时间本地推理体验很舒服</td>
    </tr>
  </tbody>
</table>

<p>简单说，Mac 的核心优势不是“无脑全能”，而是它在以下几个维度很强：</p>

<ul>
  <li><strong>大内存模型承载能力强</strong></li>
  <li><strong>解码体验好，吐字顺</strong></li>
  <li><strong>噪音低、功耗低、桌面使用幸福感高</strong></li>
</ul>

<p>但它的短板也很明确：</p>

<ul>
  <li><strong>一旦进入长上下文 / Agent / 工具频繁回写的场景，Prefill 的等待会变得很显眼</strong></li>
</ul>

<p>这也是为什么很多人会有一种“聊天真香、Agent 却没那么爽”的体验落差。</p>

<h3 id="22-三种使用场景下怎么买-mac-更合理">2.2 三种使用场景下，怎么买 Mac 更合理？</h3>

<p>如果按用户画像来分，我会把建议拆成三类：</p>

<table>
  <thead>
    <tr>
      <th>使用场景</th>
      <th>适用对象</th>
      <th>推荐机型</th>
      <th>配置重点</th>
      <th>本地模型能力</th>
      <th>预算范围</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>纯云端 AI 用户</strong></td>
      <td>主要使用 ChatGPT、Claude、Gemini 等在线服务</td>
      <td><code class="language-plaintext highlighter-rouge">Mac mini</code></td>
      <td>16GB 内存起步</td>
      <td>以云端 AI 为主，本地模型需求较低</td>
      <td>入门到中端</td>
    </tr>
    <tr>
      <td><strong>本地跑中小型模型</strong></td>
      <td>希望在本地长期运行 LLM</td>
      <td><code class="language-plaintext highlighter-rouge">Mac mini</code> / <code class="language-plaintext highlighter-rouge">Mac Studio</code></td>
      <td>32GB - 64GB 内存，更高带宽更好</td>
      <td>适合 30B 级别 MoE 或 70B 级别密集模型</td>
      <td>中端到高端</td>
    </tr>
    <tr>
      <td><strong>硬核本地大模型玩家</strong></td>
      <td>需要运行 200B+ 级别超大开源模型</td>
      <td><code class="language-plaintext highlighter-rouge">Mac Studio</code> 高内存版本</td>
      <td>256GB / 512GB 统一内存</td>
      <td>面向超大模型、本地实验与极限部署</td>
      <td>极高预算</td>
    </tr>
  </tbody>
</table>

<p>如果你只是想把 AI 真正融入日常工作流，<strong>并不一定需要一步到位堆最顶配</strong>。很多时候，决定你体验的不是参数表上的“绝对最强”，而是：</p>

<ul>
  <li>模型能不能装下；</li>
  <li>日常是否安静稳定；</li>
  <li>你是不是经常在做长上下文 Agent 任务。</li>
</ul>

<h2 id="三真正有意思的方案dgx-spark-负责-prefillmac-studio-负责-decode">三、真正有意思的方案：DGX Spark 负责 Prefill，Mac Studio 负责 Decode</h2>

<p>如果把本地推理链路拆开来看，其实会得到一个非常自然的组合思路：</p>

<ul>
  <li><strong>DGX Spark</strong>：更适合承担 <strong>Prefill</strong> 这类更依赖算力的工作</li>
  <li><strong>Mac Studio</strong>：更适合承担 <strong>Decode</strong> 这类更依赖大内存与高带宽的工作</li>
</ul>

<p>这也是我觉得最有意思的地方：<strong>Mac 未必是本地 Agent 的全能主机，但它很可能是本地 Agent 链路里体验最好的“后半程机器”。</strong></p>

<p><img src="https://raw.githubusercontent.com/yy2lyx/picgo/admin/img/mac-local-agent-cover.png" alt="mac-local-agent-cover" /></p>

<p>我这里也整理了一组测试结果。基准测试为 <strong>Llama-3.1 8B（8k 上下文）</strong>：输入 <code class="language-plaintext highlighter-rouge">8,192</code> Token 的提示词，并生成 <code class="language-plaintext highlighter-rouge">32</code> 个 Token。</p>

<table>
  <thead>
    <tr>
      <th>配置方案</th>
      <th>预填充时间</th>
      <th>生成时间</th>
      <th>总耗时</th>
      <th>提升倍数</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>DGX Spark 单机</strong></td>
      <td>1.47s</td>
      <td>2.87s</td>
      <td>4.34s</td>
      <td>1.9×</td>
    </tr>
    <tr>
      <td><strong>M3 Ultra Mac Studio 单机</strong></td>
      <td>5.57s</td>
      <td>0.85s</td>
      <td>6.42s</td>
      <td>1.0×（基准）</td>
    </tr>
    <tr>
      <td><strong>DGX Spark + M3 Ultra 组合</strong></td>
      <td><strong>1.47s</strong></td>
      <td><strong>0.85s</strong></td>
      <td><strong>2.32s</strong></td>
      <td><strong>2.8×</strong></td>
    </tr>
  </tbody>
</table>

<p>这个结果其实非常能说明问题：</p>

<ul>
  <li><strong>DGX Spark</strong> 的优势在于把前面的“读题”和建缓存做快；</li>
  <li><strong>Mac Studio</strong> 的优势在于后面的持续输出阶段足够顺；</li>
  <li>二者组合后，整体体验比单独使用任何一台都更均衡。</li>
</ul>

<p>从这个角度看，<strong>“Mac 适不适合跑本地 Agent？”</strong> 这个问题本身就应该被改写成：</p>

<blockquote>
  <p><strong>在本地 Agent 的整条推理链路里，Mac 最适合扮演什么角色？</strong></p>
</blockquote>

<p>如果你追求的是一台安静、稳定、桌面友好、能承载大模型、日常解码体验又很出色的机器，那么 Mac 依然是非常有吸引力的选择。</p>

<p>但如果你要追求的是：</p>

<ul>
  <li>极长上下文的启动速度</li>
  <li>高频工具调用后的重新预填充效率</li>
  <li>多 Agent 协同场景下的整体等待时间</li>
</ul>

<p>那么单靠 Mac，未必就是最优解。</p>

<h2 id="四结论mac-适合跑本地-agent但别用聊天体验去想象-agent-体验">四、结论：Mac 适合跑本地 Agent，但别用“聊天体验”去想象 Agent 体验</h2>

<p>最后给一个尽量短的结论：</p>

<ul>
  <li><strong>Mac 适合跑本地 Agent，但它最强的地方不是 Prefill，而是 Decode。</strong></li>
  <li><strong>如果你主要是普通对话、轻量本地推理、注重安静和桌面体验，Mac 很香。</strong></li>
  <li><strong>如果你经常跑 Coding Agent、多 Agent、长上下文任务，真正卡你的往往不是吐字，而是前面的等待时间。</strong></li>
  <li><strong>如果预算和条件允许，DGX Spark + Mac Studio 这种“前后程分工”的方案，可能才是更接近理想状态的本地 Agent 组合。</strong></li>
</ul>

<p>所以，Mac 当然适合本地 AI。</p>

<p>只是对 Agent 来说，<strong>它更像一个特别强的“后半程选手”，而不是一个全场无短板的万能答案。</strong></p>]]></content><author><name>李小肥</name><email>yeyansiwangtt@gmail.com</email></author><category term="NLP" /><category term="DeepLearning" /><summary type="html"><![CDATA[讲述agent在Mac上的使用场景和底层技术]]></summary></entry><entry><title type="html">Codex 使用指南：从一次对话，到一套可复用的开发工作流</title><link href="https://lixiaofei2yy.website/2026/09/04/Codex%E4%BD%BF%E7%94%A8.html" rel="alternate" type="text/html" title="Codex 使用指南：从一次对话，到一套可复用的开发工作流" /><published>2026-09-04T08:00:00+08:00</published><updated>2026-09-04T08:00:00+08:00</updated><id>https://lixiaofei2yy.website/2026/09/04/Codex%E4%BD%BF%E7%94%A8</id><content type="html" xml:base="https://lixiaofei2yy.website/2026/09/04/Codex%E4%BD%BF%E7%94%A8.html"><![CDATA[<h3 id="一-codex配置总览">一. codex配置总览</h3>
<blockquote>
  <p>官网：<a href="https://chatgpt.com/zh-Hans-CN/codex/">Codex</a></p>

  <p>注册：当注册的时候需要你填写电话（没有中国区电话）的时候，可以通过这个<a href="https://www.sms-activation.net/">SMS短信</a>来购买临时的虚拟电话号来接受短信完成注册。</p>
</blockquote>

<p>刚开始使用 Codex 时，很多人会把所有要求都塞进当前对话：代码风格写在 Prompt 里，项目规则靠记忆补充，常用流程每次重新描述，外部数据源则临时寻找连接方式。</p>

<p>这样当然可以完成任务，但随着项目增多，配置会逐渐变得难以维护。更好的方式是先分清楚一件事：<strong>不同类型的要求，应该放在不同的配置层。</strong></p>

<p>本文从这个问题出发，梳理 Codex 中最常用的四类配置：对话提示、<code class="language-plaintext highlighter-rouge">AGENTS.md</code>、<code class="language-plaintext highlighter-rouge">config.toml</code> 和 Skill，并进一步介绍 MCP 如何把外部工具和数据源接入工作流。读完之后，你应该能够回答三个问题：</p>

<ul>
  <li>这条要求应该写在哪里？</li>
  <li>这个能力应该属于项目，还是属于个人环境？</li>
  <li>我应该使用规则、Skill，还是 MCP？</li>
</ul>

<h4 id="11-整体配置地图">1.1 整体配置地图</h4>

<p>可以把 Codex 的配置理解成一张从“临时要求”到“长期能力”的地图：越靠近当前对话，影响范围越小、变化越快；越靠近用户全局环境，复用范围越大、稳定性要求也越高。</p>

<table>
  <thead>
    <tr>
      <th>需求</th>
      <th>推荐位置</th>
      <th>作用范围</th>
      <th>典型内容</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>当前任务的临时要求</td>
      <td>当前对话 Prompt</td>
      <td>当前任务</td>
      <td>“请只修改这个文件”</td>
    </tr>
    <tr>
      <td>团队规范和验证方式</td>
      <td>仓库或目录下的 <code class="language-plaintext highlighter-rouge">AGENTS.md</code></td>
      <td>当前目录及其子目录</td>
      <td>代码风格、测试命令、目录职责</td>
    </tr>
    <tr>
      <td>Codex 的运行默认值</td>
      <td>项目内或用户级 <code class="language-plaintext highlighter-rouge">config.toml</code></td>
      <td>项目级或用户级</td>
      <td>模型、沙箱、审批、MCP 等配置</td>
    </tr>
    <tr>
      <td>可复用的专业流程</td>
      <td><code class="language-plaintext highlighter-rouge">.agents/skills/&lt;name&gt;/SKILL.md</code> 或 <code class="language-plaintext highlighter-rouge">$CODEX_HOME/skills/&lt;name&gt;/SKILL.md</code></td>
      <td>项目级或用户级</td>
      <td>代码审查、文档生成、数据处理流程</td>
    </tr>
    <tr>
      <td>外部工具和数据源</td>
      <td><code class="language-plaintext highlighter-rouge">config.toml</code> 中的 <code class="language-plaintext highlighter-rouge">[mcp_servers.&lt;name&gt;]</code></td>
      <td>当前配置作用域</td>
      <td>数据库、搜索服务、内部 API</td>
    </tr>
  </tbody>
</table>

<p><img src="https://raw.githubusercontent.com/yy2lyx/picgo/admin/img/01_codex_configuration_layers.png" alt="Codex 配置层级示意" /></p>

<p>一个简单的判断原则是：</p>

<ol>
  <li>只在这一次任务中有效的要求，写在对话里。</li>
  <li>只对某个项目或团队有效的约定，写进项目内的 <code class="language-plaintext highlighter-rouge">AGENTS.md</code>。</li>
  <li>希望改变 Codex 运行方式的设置，写进 <code class="language-plaintext highlighter-rouge">config.toml</code>。</li>
  <li>需要反复执行、具有明确步骤的工作流，沉淀为 Skill。</li>
  <li>需要访问外部系统时，配置 MCP；MCP 的说明文字本身并不是连接配置。</li>
</ol>

<h4 id="12-运行环境配置">1.2 运行环境配置</h4>

<p>常见的配置作用域包括：</p>

<ul>
  <li>项目级：<code class="language-plaintext highlighter-rouge">项目/.codex/config.toml</code></li>
  <li>用户级：<code class="language-plaintext highlighter-rouge">~/.codex/config.toml</code></li>
</ul>

<p>项目级配置适合团队共享的项目设置，用户级配置适合个人默认值、插件和通用服务。涉及个人路径、凭据或本机差异的内容，应谨慎放入项目配置。</p>

<h3 id="二-rules配置">二. Rules配置</h3>

<h4 id="21-规则是什么">2.1 规则是什么</h4>

<p><code class="language-plaintext highlighter-rouge">AGENTS.md</code> 更像是项目交给 AI 助手的一份协作说明书。Codex 开始任务时，会在相关目录范围内查找并读取它。常见的规则来源包括：</p>

<ol>
  <li>全局规则：<code class="language-plaintext highlighter-rouge">~/.codex/AGENTS.md</code></li>
  <li>项目根目录：<code class="language-plaintext highlighter-rouge">项目/AGENTS.md</code></li>
  <li>更深层目录中的规则文件：只约束对应目录及其子目录</li>
</ol>

<p>具体的读取和合并行为会受到运行环境与目录结构影响，因此最好把项目的关键约定放在项目根目录，并在实际任务中验证 Codex 是否正确读取。</p>

<h4 id="22-规则怎么写">2.2 规则怎么写</h4>

<p>一份实用的 <code class="language-plaintext highlighter-rouge">AGENTS.md</code> 通常包含以下内容：</p>

<ul>
  <li>仓库目录职责与代码组织方式</li>
  <li>Python 命名、导入、异常处理和注释约定</li>
  <li>Spark、UDF、地理计算和数据字段规范</li>
  <li>生产环境中的敏感信息保护规则</li>
  <li>本地脚本、文档和发布流程</li>
  <li>推荐的测试、格式化和检查命令</li>
  <li>代码完成后的验收标准</li>
</ul>

<p>规则要尽量具体、可执行。例如，与其写“注意代码质量”，不如写成：</p>

<div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gu">## 验证要求</span>
<span class="p">
-</span> 修改 Python 文件后运行 <span class="sb">`pytest tests/`</span>。
<span class="p">-</span> 修改 SQL 后检查字段名、分区字段和空值处理。
<span class="p">-</span> 不得在代码中提交 Token、密码或内部服务地址。
</code></pre></div></div>

<p>规则文件的目标不是把所有背景知识都塞进去，而是让一个刚进入项目的协作者能够稳定地完成任务。对于需要多步执行的复杂流程，则更适合使用 Skill。</p>

<h3 id="三-skill配置">三. Skill配置</h3>

<h4 id="31-skill-放在哪里">3.1 Skill 放在哪里</h4>

<p>Skill 是一套可以反复调用的专业工作流。项目级 Skill 通常放在：</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>.agents/
└── skills/
    └── review-code/
        └── SKILL.md
</code></pre></div></div>

<p>创建项目级 Skill：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> .agents/skills/review-code
<span class="nb">touch</span> .agents/skills/review-code/SKILL.md
</code></pre></div></div>

<p>如果一项能力只属于 Codex，可以放在用户级目录 <code class="language-plaintext highlighter-rouge">~/.codex/skills</code>。如果希望其他 Agent 也能够使用，可以放在用户级目录 <code class="language-plaintext highlighter-rouge">~/.agents/skills</code>。用户级 Skill 通常可以被多个项目复用。</p>

<h4 id="32-一个简单的skill编写例子">3.2 一个简单的skill编写例子</h4>

<p>一个最小可用的 Skill 至少包含一个 <code class="language-plaintext highlighter-rouge">SKILL.md</code>。推荐使用 YAML 头部声明名称和用途，再写清楚执行流程：</p>

<div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">name</span><span class="pi">:</span> <span class="s">review-code</span>
<span class="na">description</span><span class="pi">:</span> <span class="s">Review code changes and report actionable findings.</span>
<span class="nn">---</span>

<span class="gh"># Review workflow</span>
<span class="p">
1.</span> Read the relevant diff and nearby code.
<span class="p">2.</span> Check tests and error handling.
<span class="p">3.</span> Report findings with file and line references.
<span class="p">4.</span> State remaining test gaps.
</code></pre></div></div>

<p><img src="https://raw.githubusercontent.com/yy2lyx/picgo/admin/img/02_skill_setup.png" alt="Skill 配置与调用示意" /></p>

<h4 id="33-codex如何调用-skill">3.3 codex如何调用 Skill</h4>

<p>最直接的方式是在对话里点名：</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>请使用 review-code skill 检查当前代码。
</code></pre></div></div>

<p>如果希望 Codex 在满足某种场景时自动考虑使用 Skill，需要在 <code class="language-plaintext highlighter-rouge">description</code> 中写清楚触发条件。例如：</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">description</span><span class="pi">:</span> <span class="s">在需要检查代码变更、测试覆盖和错误处理时使用此技能。</span>
</code></pre></div></div>

<p>这里有一个值得区分的边界：</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">AGENTS.md</code> 适合描述“这个项目始终遵守什么规则”。</li>
  <li>Skill 适合描述“完成某类任务时，要按什么步骤执行”。</li>
</ul>

<p>前者是项目约束，后者是工作流程。两者可以配合，但不应该互相替代。</p>

<h3 id="四-mcp配置">四. MCP配置</h3>

<h4 id="41-用-mcp-连接外部工具">4.1 用 MCP 连接外部工具</h4>

<p>Skill 主要告诉 Codex“如何完成一件事”，MCP 则让 Codex“能够访问什么工具或数据”。例如，数据库、搜索服务、内部 API 或其他外部系统，都可以通过 MCP 接入。</p>

<p><strong>MCP 的两类连接方式</strong></p>

<p>在 <code class="language-plaintext highlighter-rouge">config.toml</code> 中，MCP Server 常见的连接方式有两种：</p>

<ul>
  <li><strong>stdio：</strong> Codex 在本机启动一个 MCP Server 进程。</li>
  <li><strong>URL / HTTP：</strong> Codex 连接一个远程 MCP endpoint，认证方式取决于服务本身。</li>
</ul>

<p><img src="https://raw.githubusercontent.com/yy2lyx/picgo/admin/img/03_mcp_setup.png" alt="MCP 配置示意" /></p>

<h4 id="42-一个本地-mcp-server-示例">4.2 一个本地 MCP Server 示例</h4>

<p>下面是一个完整的接入本地mcp服务的例子，直接让AI帮你生成：</p>

<p><img src="https://raw.githubusercontent.com/yy2lyx/picgo/admin/img/mcpserver.png" alt="image-20260904161049651" /></p>

<p>项目级配置可以放在项目内的 <code class="language-plaintext highlighter-rouge">.codex/config.toml</code>，用户级配置可以放在 <code class="language-plaintext highlighter-rouge">~/.codex/config.toml</code>。下面是一个通过本地 Python 进程启动 MCP Server 的示例：</p>

<div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">[</span><span class="n">mcp_servers</span><span class="k">.</span><span class="n">training_sqlite</span><span class="k">]</span>
<span class="n">command</span> <span class="o">=</span><span class="w"> </span><span class="s">"~/training_sqlite_mcp/.venv/bin/python"</span>
<span class="n">args</span> <span class="o">=</span><span class="w"> </span><span class="p">[</span><span class="s">"~/training_sqlite_mcp/server.py"</span><span class="p">]</span>
<span class="n">startup_timeout_sec</span> <span class="o">=</span><span class="w"> </span><span class="mi">20</span>
</code></pre></div></div>

<p>当 Codex 启动并读取到这段配置后，就会尝试启动名为 <code class="language-plaintext highlighter-rouge">training_sqlite</code> 的本地 MCP 服务。配置完成后，建议检查三件事：</p>

<ol>
  <li><code class="language-plaintext highlighter-rouge">command</code> 指向的程序是否存在并且可执行。</li>
  <li><code class="language-plaintext highlighter-rouge">args</code> 中的脚本路径是否正确。</li>
  <li>Server 启动日志和工具列表是否符合预期。</li>
</ol>

<p>某些 MCP 客户端也会使用独立的客户端配置文件，例如 <code class="language-plaintext highlighter-rouge">~/.mcporter/mcporter.json</code>。这类文件属于对应客户端的配置，不应和 Codex 的 <code class="language-plaintext highlighter-rouge">config.toml</code> 混为一谈。使用哪种文件，取决于实际启动 MCP 的客户端和集成方式。</p>

<h3 id="五-总结">五. 总结</h3>

<h4 id="51-能力组合">5.1 能力组合</h4>

<p>一个成熟的 Codex 项目，通常不是只选其中一种配置，而是让它们各司其职：</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>当前对话 Prompt
    负责本次任务的目标和临时限制

AGENTS.md
    负责项目长期规则和验收标准

Skill
    负责可重复的专业执行流程

config.toml
    负责运行参数和外部服务连接

MCP
    提供数据库、搜索、API 等外部能力
</code></pre></div></div>

<p>例如，一个数据处理项目可以这样组织：</p>

<ul>
  <li>用 <code class="language-plaintext highlighter-rouge">AGENTS.md</code> 规定 Spark 代码风格、分区字段命名和测试命令。</li>
  <li>用 <code class="language-plaintext highlighter-rouge">review-data-pipeline</code> Skill 固化“读取代码、检查字段、验证数据质量、输出风险”的流程。</li>
  <li>用 <code class="language-plaintext highlighter-rouge">config.toml</code> 配置项目使用的 MCP Server。</li>
  <li>用 MCP 查询测试数据或读取外部元数据。</li>
  <li>在当前对话中补充本次任务的日期范围、输出位置和特殊约束。</li>
</ul>

<p>这样一来，规则、流程、运行环境和任务上下文彼此独立，后续维护也更容易定位。</p>

<h4 id="52-常见误区">5.2 常见误区</h4>

<ul>
  <li>
    <p>把所有内容都写进 Prompt：短期看最省事，长期却会导致重复劳动，也容易让不同任务之间的要求互相冲突。稳定规则应该下沉到 <code class="language-plaintext highlighter-rouge">AGENTS.md</code> 或 Skill。</p>
  </li>
  <li>
    <p>把规则写成 Skill：“所有 Python 文件都要运行测试”是项目规则，适合写进 <code class="language-plaintext highlighter-rouge">AGENTS.md</code>；“如何进行一次完整的代码审查”才是工作流，适合写进 Skill。</p>
  </li>
  <li>
    <p>把 MCP 说明当成 MCP 配置：一段介绍 MCP 的文字不会自动创建连接。真正的连接需要配置 Server、启动命令或远程 endpoint，并且要验证服务是否成功启动。</p>
  </li>
</ul>

<h4 id="53-一份实用的落地顺序">5.3 一份实用的落地顺序</h4>

<p>如果你正在从零整理自己的 Codex 环境，可以按下面的顺序开始：</p>

<ol>
  <li>先在当前对话中验证需求和工作方式。</li>
  <li>把项目中稳定、必须遵守的约定整理到 <code class="language-plaintext highlighter-rouge">AGENTS.md</code>。</li>
  <li>把重复出现的多步骤任务整理为 Skill。</li>
  <li>再通过项目级或用户级 <code class="language-plaintext highlighter-rouge">config.toml</code> 固定运行环境。</li>
  <li>最后接入真正需要访问的 MCP Server，并单独验证启动和调用。</li>
</ol>]]></content><author><name>李小肥</name><email>yeyansiwangtt@gmail.com</email></author><category term="NLP" /><category term="DeepLearning" /><summary type="html"><![CDATA[讲述对codex使用过程总结]]></summary></entry><entry><title type="html">打造属于自己的免费Cursor：claude code+ollama+vscode</title><link href="https://lixiaofei2yy.website/2026/05/26/%E6%90%AD%E5%BB%BA%E8%87%AA%E5%B7%B1%E7%9A%84cursor_claudecode_ollama_vs.html" rel="alternate" type="text/html" title="打造属于自己的免费Cursor：claude code+ollama+vscode" /><published>2026-05-26T08:00:00+08:00</published><updated>2026-05-26T08:00:00+08:00</updated><id>https://lixiaofei2yy.website/2026/05/26/%E6%90%AD%E5%BB%BA%E8%87%AA%E5%B7%B1%E7%9A%84cursor_claudecode_ollama_vs</id><content type="html" xml:base="https://lixiaofei2yy.website/2026/05/26/%E6%90%AD%E5%BB%BA%E8%87%AA%E5%B7%B1%E7%9A%84cursor_claudecode_ollama_vs.html"><![CDATA[<h3 id="一-ai-coding软件的横向对比">一. AI Coding软件的横向对比</h3>

<table>
  <thead>
    <tr>
      <th>工具名称</th>
      <th>厂商</th>
      <th>付费模式（个人）</th>
      <th>模型能力（默认 / 上限）</th>
      <th>核心特色</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Cursor</strong></td>
      <td>Anysphere（美国）</td>
      <td>免费版（基础）；<strong>Pro $20 / 月</strong>；企业定制</td>
      <td>默认：<strong>Claude 3.5/4 Sonnet/Opus</strong>；可接 GPT-5、Gemini；长上下文强（200K+）</td>
      <td>VS Code 魔改 IDE；全仓库索引 + 实时代码编辑；<strong>AI 体验最顺滑</strong>；国内网络友好；生态插件多</td>
    </tr>
    <tr>
      <td><strong>Codex</strong></td>
      <td>OpenAI（美国）</td>
      <td>网页版：ChatGPT Plus <strong>$20/月</strong>；CLI：开源免费；Pro $120 / 月</td>
      <td>仅限 <strong>GPT-5.3/5.4 Codex</strong>；推理强、代码准确率高；上下文 128K</td>
      <td>云端沙箱安全执行；异步批量任务强；CLI 轻量；适合<strong>自动化脚本、批量生成</strong>；封闭生态</td>
    </tr>
    <tr>
      <td><strong>Trae</strong></td>
      <td>字节（中国）</td>
      <td>基础版<strong>永久免费</strong>；Pro <strong>¥60 / 月（≈$10）</strong></td>
      <td>自研 <strong>Doubao 大模型</strong>；兼容 GPT-4/5、Claude、本地 Ollama；中文理解强、长文本友好</td>
      <td>国产 AI IDE（VS Code 分支）；<strong>中文优化 + 国内速度快</strong>；本地优先、隐私友好；适合国内团队 / 学生</td>
    </tr>
    <tr>
      <td><strong>CodeBuddy</strong></td>
      <td>腾讯（中国）</td>
      <td>基础免费；Pro <strong>¥40 / 月</strong>；学生特惠</td>
      <td>自研 <strong>GLM-4/5</strong>；可接入 GPT-4/5、Claude、Ollama；编码 + 工具调用均衡</td>
      <td>轻量 IDE 插件 + 独立客户端；<strong>国内合规 + 数据不出境</strong>；文档 / 注释生成强；适合政企 / 学术场景</td>
    </tr>
    <tr>
      <td><strong>OpenCode</strong></td>
      <td>开源</td>
      <td><strong>完全开源免费</strong>；仅付第三方 API 费用</td>
      <td>无绑定：支持 <strong>75+ LLM</strong>（Claude、GPT、Gemini、Kimi、Ollama）；本地 / 云端自由切换</td>
      <td>Claude Code 开源平替；<strong>TUI 终端界面友好</strong>；多会话 Agent；国内访问稳定；高度可定制</td>
    </tr>
  </tbody>
</table>

<p>说实话，如果咱们有钱，那肯定<strong>无脑入cursor</strong>啊，奈何钱包有限，只能用免费方案了，那么下面就是一种<strong>免费平替</strong>方案：</p>

<ul>
  <li>大模型：利用ollama的免费cloud模型或者直接使用本地模型</li>
  <li>Coding Agent：这里选择claude code作为coding agent</li>
  <li>界面：选择强大的vscode</li>
</ul>

<h3 id="二-本地安装相关软件">二. 本地安装相关软件</h3>

<blockquote>
  <p>claude code 文档：https://code.claude.com/docs/en/overview</p>

  <p>Ollama 官网：https://ollama.com</p>
</blockquote>

<h4 id="21-下载必须软件">2.1 下载必须软件</h4>

<p>Claude code 安装：</p>

<ul>
  <li>下载Mac：<code class="language-plaintext highlighter-rouge">curl -fsSL https://claude.ai/install.sh | bash</code></li>
  <li>给zshrc加可写权限：<code class="language-plaintext highlighter-rouge">sudo chmod 777 ~/.zshrc</code></li>
  <li>加载环境变量：<code class="language-plaintext highlighter-rouge">echo 'export PATH="$HOME/.local/bin:$PATH"' &gt;&gt; ~/.zshrc &amp;&amp; source ~/.zshrc</code></li>
</ul>

<p>ollama安装：<code class="language-plaintext highlighter-rouge">curl -fsSL https://ollama.com/install.sh | sh</code></p>

<h4 id="22-ollama模型的选型">2.2 ollama模型的选型</h4>

<p>针对coding场景，ollama适配claude code的模型需要具备的能力：</p>

<ul>
  <li>支持ools/function calling</li>
  <li>长上下文：这里推荐至少 32k</li>
  <li>适合编码</li>
  <li>最好能支持图片</li>
</ul>

<p>针对下面不同的场景，这里推荐的ollama模型：</p>

<table>
  <thead>
    <tr>
      <th>使用诉求</th>
      <th>推荐模型</th>
      <th>一键启动命令</th>
      <th>厂商 + 简介</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>离线 / 隐私优先、低配电脑</td>
      <td>qwen2.5-coder:7b</td>
      <td><code class="language-plaintext highlighter-rouge">ollama launch claude --model qwen2.5-coder:7b</code></td>
      <td>阿里巴巴（通义千问）：轻量强编码、工具调用稳、本地首选</td>
    </tr>
    <tr>
      <td>免费云端主力（首选）</td>
      <td>minimax-m2.5:cloud</td>
      <td><code class="language-plaintext highlighter-rouge">ollama launch claude --model minimax-m2.5:cloud</code></td>
      <td>MiniMax（稀宇科技）：国内速度快、Claude Code 适配好、免费最优</td>
    </tr>
    <tr>
      <td>长代码 / 大项目（免费）</td>
      <td>kimi-k2.5:cloud</td>
      <td><code class="language-plaintext highlighter-rouge">ollama launch claude --model kimi-k2.5:cloud</code></td>
      <td>Moonshot（月之暗面）：百万级上下文、读大库强</td>
    </tr>
    <tr>
      <td>重度开发、不限额度（Pro 订阅）</td>
      <td>minimax-m2.7:cloud</td>
      <td><code class="language-plaintext highlighter-rouge">ollama launch claude --model minimax-m2.7:cloud</code></td>
      <td>MiniMax：M2.5 升级版、Agent 更强、付费主力</td>
    </tr>
    <tr>
      <td>架构 / 底层开发（Pro 订阅）</td>
      <td>deepseek-v4-pro:cloud</td>
      <td><code class="language-plaintext highlighter-rouge">ollama launch claude --model deepseek-v4-pro:cloud</code></td>
      <td>DeepSeek（深度求索）：顶尖推理 + 架构能力、复杂系统首选</td>
    </tr>
  </tbody>
</table>

<h3 id="三--在vscode中使用claude-code">三.  在VScode中使用claude code</h3>

<h4 id="31-vscode配置claude-code">3.1 vscode配置claude code</h4>

<p>打开vscode的plugins，然后搜索<code class="language-plaintext highlighter-rouge">Claude Code for VS Code</code>这个插件，进行安装。</p>

<p>claude code在vscode中使用的是2种方式，这里可以使用<code class="language-plaintext highlighter-rouge">cmd</code> + <code class="language-plaintext highlighter-rouge">shift</code> +<code class="language-plaintext highlighter-rouge">p</code>，然后选择<code class="language-plaintext highlighter-rouge">Claude Code: Open In New Tap</code>来进行切换：</p>

<ul>
  <li><strong>CLI</strong>：即命令行模式，<strong>说实话其实也很好看</strong>。</li>
  <li><strong>原生UI</strong>：使用命令行可能大多数人在界面上不适应，这里可以选择使用原生UI。</li>
</ul>

<h4 id="32-配置claude-code的ollama模型">3.2 配置Claude code的ollama模型</h4>

<p><strong>临时在CLI中使用ollama模型</strong></p>

<p>当我们直接在命令行中输入<code class="language-plaintext highlighter-rouge">claude</code>后，会发现模型还是claude自带收费模型<code class="language-plaintext highlighter-rouge">Opus 4.7 (1M context)</code>，我们这里需要推出后，使用ollama模型：<code class="language-plaintext highlighter-rouge">claude --model minimax-m2.5:cloud</code>，就可以发现你可以直接在vscode中使用ollama模型的claude了，如下图。</p>

<p><img src="/assets/images/claude_cli.png" alt="claude_cli" /></p>

<p><strong>永久在原生UI和CLI中使用ollama模型</strong></p>

<p>vscode 设置全局变量<code class="language-plaintext highlighter-rouge">settings.json</code>，这里需要将可选的模型和url改成ollama的模型：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>{
    "claudeCode.preferredLocation": "panel",
    "terminal.integrated.inheritEnv": false,
    "terminal.integrated.mouseWheelScrollSensitivity": 3,
    "claudeCode.environmentVariables": [
    
        {
            "name": "ANTHROPIC_BASE_URL",
            "value": "http://localhost:11434"
        },
        {
            "name": "ANTHROPIC_AUTH_TOKEN",
            "value": "ollama"
        },
        {
            "name": "ANTHROPIC_DEFAULT_HAIKU_MODEL",
            "value": "minimax-m2.5:cloud"
        },
        {
            "name": "ANTHROPIC_DEFAULT_SONNET_MODEL",
            "value": "minimax-m2.5:cloud"
        },
        {
            "name": "ANTHROPIC_DEFAULT_OPUS_MODEL",
            "value": "minimax-m2.5:cloud"
        },

    ]
}
</code></pre></div></div>

<p>配置完上述的环境变量后，当你再次打开vscode后就发现默认使用的就是ollama的<code class="language-plaintext highlighter-rouge">minimax-m2.5:cloud</code>模型了。</p>

<p><img src="/assets/images/claude_ui.png" alt="claude_ui" /></p>]]></content><author><name>李小肥</name><email>yeyansiwangtt@gmail.com</email></author><category term="NLP" /><category term="DeepLearning" /><summary type="html"><![CDATA[讲述利用claude code和ollama来打造属于自己免费的coding agent]]></summary></entry><entry><title type="html">从 LLM 到 Agent：原理、架构与协作模式全景</title><link href="https://lixiaofei2yy.website/2026/05/22/%E4%BB%8ELLM%E5%88%B0Agent.html" rel="alternate" type="text/html" title="从 LLM 到 Agent：原理、架构与协作模式全景" /><published>2026-05-22T08:00:00+08:00</published><updated>2026-05-22T08:00:00+08:00</updated><id>https://lixiaofei2yy.website/2026/05/22/%E4%BB%8ELLM%E5%88%B0Agent</id><content type="html" xml:base="https://lixiaofei2yy.website/2026/05/22/%E4%BB%8ELLM%E5%88%B0Agent.html"><![CDATA[<h3 id="一-agent的由来">一. Agent的由来</h3>

<h4 id="11-什么是agent">1.1 什么是Agent</h4>

<p>这里使用Claude Code作为例子来解释Agent到底是什么</p>

<p><img src="https://raw.githubusercontent.com/yy2lyx/picgo/admin/img/claudcode.png" alt="claudcode" /></p>

<p>我们可以将Claude Code当作为一个Coding Agent：</p>

<ul>
  <li>没有Coding Agent：LLM模型没有相关的权限和工具来解决用户提出的问题</li>
  <li>有Coding Agent：
    <ul>
      <li><strong>用户提问</strong>：直接问 “garden.jpg 里有几朵花？”</li>
      <li>Claude Code 自动调用工具：
        <ul>
          <li>用 <code class="language-plaintext highlighter-rouge">read file</code> 工具读取本地图片文件，获取文件内容</li>
          <li>将图片内容传给 Claude Opus，进行视觉分析。如果这里是其他复杂任务，这里Coding Agent才会自动生成并运行脚本</li>
          <li>Claude Opus 分析后，直接返回 “找到了 5 朵花”</li>
        </ul>
      </li>
    </ul>
  </li>
</ul>

<p>因此，Agent具备<strong>三要素</strong>：</p>

<ul>
  <li>Prompts：
    <ul>
      <li>角色是什么</li>
      <li>能做什么</li>
      <li>不能做什么</li>
    </ul>
  </li>
  <li>Context Management：
    <ul>
      <li>有限的context window</li>
      <li>对话历史</li>
      <li>RAG匹配的数据</li>
    </ul>
  </li>
  <li>Tools：
    <ul>
      <li>做事的：比如订酒店，输出文档等</li>
      <li>查询的：查询数据库、天气</li>
    </ul>
  </li>
</ul>

<h4 id="12-llm为什么需要agentharness">1.2 LLM为什么需要Agent—Harness</h4>

<p>LangChain 团队 “用 LLM+Agent 打败纯 LLM”，最典型、最权威的公开案例是：<strong>在不换底层模型的前提下，通过给 LLM 套上 Agent 框架 + 系统化工程（Harness Engineering），把编码智能体成绩从 52.8% 提升到 66.5%，从榜单 30 名开外打进前 5 名</strong>。</p>

<p>纯LLM有以下天然的<strong>短板（52.8%）</strong>：</p>

<ul>
  <li><strong>知识截止</strong>：不会查资料，硬编命令，知识也截止到LLM训练的预料时间节点内。</li>
  <li><strong>幻觉</strong>：没见过的，可能硬生成虚无缥缈的东西。</li>
  <li><strong>偏离路径</strong>：过于发散，生成的结果可能会飘的很远。</li>
  <li><strong>没有分步规划</strong>：复杂任务（调试、多文件、环境配置）一步到位，容易崩。</li>
  <li><strong>无法使用工具</strong>：无法调用其他工具来执行指令。</li>
</ul>

<p>Langchain团队的<strong>Harness Engineering（66.5%）</strong>：</p>

<ol>
  <li>强化 System Prompt（给 Agent 一本 “工作手册”）</li>
</ol>

<ul>
  <li>明确要求<strong>分步思考、先规划再行动、必须校验结果</strong>。</li>
  <li>强制格式：每次输出必须包含 <code class="language-plaintext highlighter-rouge">Thought</code>、<code class="language-plaintext highlighter-rouge">Action</code>、<code class="language-plaintext highlighter-rouge">Action Input</code>，便于解析和执行。</li>
  <li>禁止幻觉：<strong>不知道就去查，不许瞎编</strong>。</li>
</ul>

<ol>
  <li>接入强工具集（让 LLM 有手脚）</li>
</ol>

<ul>
  <li><strong>Bash 工具</strong>：执行命令、安装依赖、运行程序、看报错。</li>
  <li><strong>文件工具</strong>：读 / 写 / 编辑代码、配置文件。</li>
  <li><strong>Python REPL</strong>：运行代码、算结果、调试片段。</li>
  <li><strong>搜索工具</strong>：查文档、查报错、查最新 API。</li>
</ul>

<ol>
  <li>加中间件 / 钩子（Middleware/Hooks）—— 防崩、防死循环</li>
</ol>

<ul>
  <li><strong>超时控制</strong>：单步超时自动终止，避免卡死。</li>
  <li><strong>死循环检测</strong>：连续 3 步重复动作 → 强制换策略或终止。</li>
  <li><strong>结果校验</strong>：关键步骤（如编译、运行）自动检查返回码 / 输出，失败自动重试或回退。</li>
  <li><strong>错误回溯</strong>：保存失败前状态，支持 “回到上一步换方法”。</li>
</ul>

<ol>
  <li>用 LangSmith 做迭代优化（数据驱动）</li>
</ol>

<ul>
  <li>跑大量测试，<strong>追踪每一步 Thought/Action/Observation</strong>。</li>
  <li>分析失败模式：是规划错、工具用错、校验漏了，还是 Prompt 不好。</li>
  <li>针对性改 Prompt、工具、流程 → 再测 → 再分析，闭环优化。</li>
</ul>

<h3 id="二-agent的多种模式">二. Agent的多种模式</h3>

<blockquote>
  <p>这里推荐的是一片git代码，对agent的多种模式讲述的非常清晰：https://github.com/FareedKhan-dev/all-agentic-architectures，下面是针对这段代码的解读</p>
</blockquote>

<h4 id="21-基础增强模式让单-agent-更聪明-">2.1 基础增强模式：让单 Agent 更聪明 🧠</h4>

<p>让一个 Agent 从「直接出答案」进化到「能反思、能用工具、能规划」。</p>

<table>
  <thead>
    <tr>
      <th>模式</th>
      <th>agent本质</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>01 Reflection（反思）</strong></td>
      <td>生成 → 自我批判 → 修正，循环改稿</td>
    </tr>
    <tr>
      <td><strong>02 Tool Use（工具调用）</strong></td>
      <td>让 LLM 调外部 API/函数突破知识截止</td>
    </tr>
    <tr>
      <td><strong>03 ReAct（推理+行动）</strong></td>
      <td>Thought → Action → Observation 交错循环</td>
    </tr>
    <tr>
      <td><strong>04 Planning（规划）</strong></td>
      <td>先列好完整步骤，再按计划执行</td>
    </tr>
  </tbody>
</table>

<p><strong>典型流程</strong>：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Reflection: Generate → Critique → Refine
ReAct:      Thought ⇄ Action ⇄ Observation → Final
Planning:   Plan → Execute step by step → Aggregate
</code></pre></div></div>

<p><strong>何时用</strong>：单一 Agent 任务，输出质量、信息时效或步骤复杂度需要提升。</p>

<h4 id="22-多-agent-协作让-agent-像团队一样工作-">2.2 多 Agent 协作：让 Agent 像团队一样工作 👥</h4>

<p>把任务交给一组分工的专家，或并行多视角，或动态路由。</p>

<table>
  <thead>
    <tr>
      <th>模式</th>
      <th>agent本质</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>05 Multi-Agent</strong></td>
      <td>固定流水线分工（PM → 架构师 → Coder → Reviewer）</td>
    </tr>
    <tr>
      <td><strong>07 Blackboard</strong></td>
      <td>所有 Agent 共享黑板，控制器机会式调度</td>
    </tr>
    <tr>
      <td><strong>11 Meta-Controller</strong></td>
      <td>主管 Agent 把任务路由给最合适的专家</td>
    </tr>
    <tr>
      <td><strong>13 Ensemble</strong></td>
      <td>多 Agent 并行独立分析 + Aggregator 综合</td>
    </tr>
  </tbody>
</table>

<p><strong>对比要点</strong>：</p>

<ul>
  <li><strong>Multi-Agent</strong>：流程固定 → 软件开发流水线</li>
  <li><strong>Meta-Controller</strong>：智能路由 → 多功能 AI 平台一个入口</li>
  <li><strong>Blackboard</strong>：开放协作 → 复杂诊断、动态情境</li>
  <li><strong>Ensemble</strong>：投票式 → 高风险决策、降低偏见</li>
</ul>

<p><strong>何时用</strong>：单一 Agent 撑不起任务，需要专业分工或多视角综合。</p>

<h4 id="23-高级记忆与推理让-agent-想得更深记得更久-">2.3 高级记忆与推理：让 Agent 想得更深、记得更久 🧬</h4>

<p>赋予 Agent 长期记忆与复杂关系推理能力。</p>

<table>
  <thead>
    <tr>
      <th>模式</th>
      <th>Agent本质</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>08 Episodic + Semantic Memory</strong></td>
      <td>向量库记对话 + 图库记事实，双记忆系统</td>
    </tr>
    <tr>
      <td><strong>09 Tree of Thoughts</strong></td>
      <td>推理建模为树搜索，多分支评估剪枝</td>
    </tr>
    <tr>
      <td><strong>12 Graph（世界模型）</strong></td>
      <td>知识存为「实体 + 关系」图，多跳遍历推理</td>
    </tr>
  </tbody>
</table>

<p><strong>记忆 vs 推理</strong>：</p>

<ul>
  <li><strong>08 + 12</strong> 偏「记得更多」（FAISS 向量 / Neo4j 图谱）</li>
  <li><strong>09</strong> 偏「想得更深」（指数级思维分支搜索）</li>
</ul>

<p><strong>何时用</strong>：</p>

<ul>
  <li>长期个性化助手 → 08</li>
  <li>关系密集型问答（投资关系、文献引用）→ 12</li>
  <li>强约束逻辑/数学难题（狼羊菜过河类）→ 09</li>
</ul>

<h4 id="24-安全与可靠让-agent-真敢上生产-️">2.4 安全与可靠：让 Agent 真敢上生产 🛡️</h4>

<p>为 Agent 装上「三思而后行」与「自知之明」的护栏。</p>

<table>
  <thead>
    <tr>
      <th>模式</th>
      <th>Agent本质</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>06 PEV（Plan-Execute-Verify）</strong></td>
      <td>每步执行后由 Verifier 检查，错了就重规划</td>
    </tr>
    <tr>
      <td><strong>10 Mental Loop（心智仿真）</strong></td>
      <td>在沙盒中先模拟，再决定真实动作</td>
    </tr>
    <tr>
      <td><strong>14 Dry-Run Harness</strong></td>
      <td>干跑 + 人审批，再真实执行</td>
    </tr>
    <tr>
      <td><strong>17 Reflexive Metacognitive</strong></td>
      <td>维护「自我模型」，能说”我不知道，建议找人”</td>
    </tr>
  </tbody>
</table>

<p><strong>安全光谱</strong>：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PEV          → 自动错误恢复
Mental Loop  → Agent 自己模拟自己审
Dry-Run      → 人类把关，最强护栏
Metacognitive→ 越界即拒绝/转交人类
</code></pre></div></div>

<p><strong>何时用</strong>：金融、医疗、法律、生产环境的不可逆操作；任何「错答即灾难」的领域。</p>

<h4 id="25-学习与适应让-agent-越用越好-">2.5 学习与适应：让 Agent 越用越好 📈</h4>

<p>通过反馈与涌现机制，让系统自我演进。</p>

<table>
  <thead>
    <tr>
      <th>模式</th>
      <th>Agent本质</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>15 RLHF（自我改进）</strong></td>
      <td>Editor 批评 Author，沉淀高质量样本到金标库</td>
    </tr>
    <tr>
      <td><strong>16 Cellular Automata</strong></td>
      <td>网格上多个简单 Agent 用局部规则涌现复杂行为</td>
    </tr>
  </tbody>
</table>

<p><strong>两种适应方式</strong>：</p>

<ul>
  <li><strong>15</strong> 集中式：靠反馈闭环逐步优化（适合内容生产、代码生成）</li>
  <li><strong>16</strong> 分布式：靠涌现解决空间/规模问题（适合路径规划、群体仿真）</li>
</ul>

<p><strong>何时用</strong>：长期运行、需持续优化的系统；或大规模、高鲁棒性的分布式问题。</p>

<h4 id="26-选型速查">2.6 选型速查</h4>

<table>
  <thead>
    <tr>
      <th>你的需求</th>
      <th>推荐</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>输出质量不够</td>
      <td>01 Reflection / 15 RLHF</td>
    </tr>
    <tr>
      <td>要查实时信息</td>
      <td>02 Tool Use / 03 ReAct</td>
    </tr>
    <tr>
      <td>任务步骤多且复杂</td>
      <td>04 Planning / 06 PEV</td>
    </tr>
    <tr>
      <td>需要专家团队</td>
      <td>05 Multi-Agent / 11 Meta-Controller</td>
    </tr>
    <tr>
      <td>多视角降低偏见</td>
      <td>13 Ensemble</td>
    </tr>
    <tr>
      <td>灵活开放式协作</td>
      <td>07 Blackboard</td>
    </tr>
    <tr>
      <td>长期个性化</td>
      <td>08 Episodic+Semantic</td>
    </tr>
    <tr>
      <td>关系/逻辑深推理</td>
      <td>12 Graph / 09 ToT</td>
    </tr>
    <tr>
      <td>不可逆动作要安全</td>
      <td>14 Dry-Run / 10 Mental Loop / 06 PEV</td>
    </tr>
    <tr>
      <td>高风险领域咨询</td>
      <td>17 Metacognitive</td>
    </tr>
    <tr>
      <td>大规模空间问题</td>
      <td>16 Cellular Automata</td>
    </tr>
  </tbody>
</table>

<h3 id="三-multi-agent">三. Multi Agent</h3>

<h4 id="31-单agent和multi-agent的差异">3.1 单agent和multi agent的差异</h4>

<p>一般而言，我们是鼓励使用单agent来解题，下面是单agent和multi agent的在定差异：</p>

<ul>
  <li><strong>单agent</strong>：单个智能体独立思考、规划、调用工具、闭环完成全流程任务</li>
  <li><strong>multi agent</strong>：多个分工智能体协同，各司其职、消息交互、分工协作完成复杂任务</li>
</ul>

<p>核心差异如下：</p>

<table>
  <thead>
    <tr>
      <th>对比项</th>
      <th>单 Agent</th>
      <th>多 Agent</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>分工模式</td>
      <td>一人全包，无角色拆分</td>
      <td>按职责拆分角色，各司其职</td>
    </tr>
    <tr>
      <td>决策逻辑</td>
      <td>统一大脑决策，串行执行</td>
      <td>多角色协商、表决、分工决策</td>
    </tr>
    <tr>
      <td>资源调用</td>
      <td>独占工具与上下文</td>
      <td>工具分片使用，信息互通共享</td>
    </tr>
    <tr>
      <td>上下文复杂度</td>
      <td>低，任务链路简单</td>
      <td>高，多角色会话交互</td>
    </tr>
    <tr>
      <td>开发成本</td>
      <td>低，逻辑简洁易维护</td>
      <td>高，需设计角色、通信、权限</td>
    </tr>
    <tr>
      <td>容错能力</td>
      <td>单点故障任务中断</td>
      <td>可替补分工，容错性更强</td>
    </tr>
  </tbody>
</table>

<h4 id="32-multi-agent的2个场景例子">3.2 Multi agent的2个场景例子</h4>

<p><strong>case1-平行处理: 旅游场景，需要参考和建议下航班，酒店和天气等情况</strong></p>

<p>在这个场景中：</p>

<ul>
  <li>单agent：找航班 -&gt; 找酒店 -&gt; 看天气，需要<strong>agent一个一个做</strong>。</li>
  <li>Muti agent：设定三个agent，分别是订航班agent、订酒店agent、查天气agent，<strong>三个agent并行处理</strong>。</li>
</ul>

<p><strong>case2-层级处理: 智慧家居场景，其中包含中央节点通过层级的方式来管理、控制其他的agent</strong></p>

<ul>
  <li><strong>Orchestrator（中央大脑）</strong>：是层级（Hierarchical）主节点，负责接收用户指令、统筹任务、协调灯光 / 保全 / 温控 / 能源等设备 Agent。</li>
  <li><strong>A2A（agent-to-agent，点对点）</strong>：是<strong>设备 Agent 之间的直接通信链路</strong>，不经过中央 Orchestrator，直接交互。</li>
</ul>]]></content><author><name>李小肥</name><email>yeyansiwangtt@gmail.com</email></author><category term="NLP" /><category term="DeepLearning" /><summary type="html"><![CDATA[讲述agent的原理、架构与协作模式全景]]></summary></entry><entry><title type="html">Function call、MCP、SKILL下哪个才是大模型的最优解</title><link href="https://lixiaofei2yy.website/2026/05/19/function-call_MCP_SKILL%E4%B8%8B%E5%93%AA%E4%B8%AA%E6%89%8D%E6%98%AF%E5%A4%A7%E6%A8%A1%E5%9E%8B%E7%9A%84%E6%9C%80%E4%BC%98%E8%A7%A3.html" rel="alternate" type="text/html" title="Function call、MCP、SKILL下哪个才是大模型的最优解" /><published>2026-05-19T08:00:00+08:00</published><updated>2026-05-19T08:00:00+08:00</updated><id>https://lixiaofei2yy.website/2026/05/19/function%20call_MCP_SKILL%E4%B8%8B%E5%93%AA%E4%B8%AA%E6%89%8D%E6%98%AF%E5%A4%A7%E6%A8%A1%E5%9E%8B%E7%9A%84%E6%9C%80%E4%BC%98%E8%A7%A3</id><content type="html" xml:base="https://lixiaofei2yy.website/2026/05/19/function-call_MCP_SKILL%E4%B8%8B%E5%93%AA%E4%B8%AA%E6%89%8D%E6%98%AF%E5%A4%A7%E6%A8%A1%E5%9E%8B%E7%9A%84%E6%9C%80%E4%BC%98%E8%A7%A3.html"><![CDATA[<h3 id="一-function-call和mcp">一. Function Call和MCP</h3>

<h4 id="11-function-call">1.1 Function Call</h4>

<p><strong>Function Call 是大模型原生具备的「结构化输出能力」，用来让模型主动请求外部函数 / API。</strong></p>

<ul>
  <li>
    <p>起源：2023 年 OpenAI GPT-3.5/4 推出</p>
  </li>
  <li>
    <p>本质：<strong>模型输出格式</strong>（JSON），不是协议</p>
  </li>
  <li>
    <p>工作方式：</p>

    <ol>
      <li>你给模型「函数列表 + 参数定义」
  ```
  使用工具：
  如果遇到你的知识无法回答的问题，使用工具，把使用工具的指令放在<tool>和</tool>中间，使用完工具后你会得到输出放在 <output>和</output>中间</li>
    </ol>

    <p>特定使用工具的形式：
  现在你可以使用的工具如下：查询某地、某时问题的函数 Temperature（location, time），使用范例：Temperature（’北京’，’2026-02-22 14:26”）</p>
    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
2. 用户提问 → 模型判断要调用哪个函数

3. 模型输出类似：

</code></pre></div>    </div>
    <p>{“name”:”get_weather”,”arguments”:{“city”:”武汉”}}
   ```</p>

    <ol>
      <li>后端执行函数 → 结果回传给模型 → 模型生成回答</li>
    </ol>
  </li>
</ul>

<h4 id="12-mcp">1.2 MCP</h4>

<p><strong>MCP（Model Context Protocol，模型上下文协议）是 Anthropic 2024 年底开源的「开放、标准化、双向通信协议」，用于连接任意 LLM 与任意工具 / 数据源。</strong></p>

<ul>
  <li>
    <p>本质：<strong>跨模型、跨工具的通用通信标准（像 USB‑C）</strong></p>
  </li>
  <li>
    <p>架构：Client ↔ Host ↔ Server（解耦、双向）</p>
  </li>
  <li>
    <p>能力不止函数调用：</p>

    <ul>
      <li>
        <p><strong>Tools</strong>：调用工具（类似 Function Call）</p>
      </li>
      <li>
        <p><strong>Resources</strong>：直接挂载大文件 / 数据库 / 代码库</p>
      </li>
      <li>
        <p><strong>Prompts</strong>：服务端下发提示词模板</p>
      </li>
      <li>
        <p>会话管理、状态保持、错误处理、双向调用</p>
      </li>
    </ul>
  </li>
</ul>

<h4 id="13-两者核心区别">1.3 两者核心区别</h4>

<p><strong>Function Call</strong>：你告诉模型怎么调用工具，那么就肯定包含了一下这些必须定义的内容</p>

<ul>
  <li>tool 定义规范</li>
  <li>定义入参和出参argument的结构</li>
  <li>写错误处理（timeout、重试、异常）</li>
  <li>每个模型（OpenAI / 通义 / 文心）格式不一样，还要<strong>适配 N 套</strong></li>
</ul>

<p>MCP：工具自己告诉模型我能做什么，相当于上述需要自定义的内容直接不用做了，<strong>全自动</strong>了属于是。</p>

<p><img src="https://raw.githubusercontent.com/yy2lyx/picgo/admin/img/mcp.png" alt="" /></p>

<h4 id="14-两者适用场景">1.4 两者适用场景</h4>

<p>下面是用表格的形式来展示2者的不同：</p>

<table>
  <thead>
    <tr>
      <th>特性</th>
      <th>Function Call</th>
      <th>MCP</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Setup（搭建方式）</strong></td>
      <td>手动配置，硬编码嵌入到你的应用中</td>
      <td>工具可注册，支持自动发现</td>
    </tr>
    <tr>
      <td><strong>Scalability（可扩展性）</strong></td>
      <td>扩展困难；每新增一个工具都需要编写新代码</td>
      <td>扩展性强；即插即用的工具生态</td>
    </tr>
    <tr>
      <td><strong>Maintenance（维护成本）</strong></td>
      <td>所有逻辑都需要你自行管理</td>
      <td>简化维护；由协议本身处理通用逻辑</td>
    </tr>
    <tr>
      <td><strong>Best For（适用场景）</strong></td>
      <td>简单、单一用途的应用</td>
      <td>复杂、多智能体、企业级系统</td>
    </tr>
  </tbody>
</table>

<h3 id="二-skills">二. Skills</h3>

<p>skills其实是用自然语言的方式记录一套固定的流程手册或者领域的SOP，它主要是将一套agent的工作流封装成一个skill，<strong>让AI更清晰、不偏的、按照步骤的一步一步将任务做对</strong>。</p>

<h4 id="21-核心定位">2.1 核心定位</h4>

<p>MCP</p>

<ul>
  <li>层级：<strong>底层集成层 / 协议层</strong></li>
  <li>角色：<strong>通用、中立、跨模型、跨工具</strong>的连接标准</li>
  <li>不关心业务：只定义 <strong>工具发现、调用格式、参数、错误处理、权限、状态</strong></li>
  <li>一句话：<strong>MCP 管 “能不能连、怎么连”</strong></li>
</ul>

<p>Skills</p>

<ul>
  <li>层级：<strong>上层知识层 / 执行层</strong></li>
  <li>角色：<strong>领域专属、任务导向、可复用的技能包</strong>（本质是结构化提示词 + 流程）</li>
  <li>关心业务：定义<strong>触发条件、步骤、判断逻辑、输出格式、边界处理、示例</strong></li>
  <li>一句话：<strong>Skills 管 “会不会做、怎么做对”</strong></li>
</ul>

<h4 id="22-如何写skills">2.2 如何写Skills</h4>

<p><strong>不需要</strong>关心底层连接，但必须写：</p>

<ul>
  <li><strong>Skill 触发条件</strong>（什么时候用这个技能）</li>
  <li><strong>完整步骤 SOP</strong>（先做什么、后做什么、判断分支）</li>
  <li><strong>参数要求、输出模板、示例、边界处理</strong></li>
  <li>本质：<strong>写一份给 AI 看的详细操作手册</strong></li>
</ul>

<p>下面是一个典型的skills的目录结构：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>.claude/skills
├── pdf-parsing
│   ├── script.py
│   └── SKILL.md
├── python-code-style
│   ├── REFERENCE.md
│   └── SKILL.md
└── web-scraping
    └── SKILL.md
</code></pre></div></div>

<p>对于每个skill，必须要包含<code class="language-plaintext highlighter-rouge">SKILL.md</code>文件，下面是一个<code class="language-plaintext highlighter-rouge">SKILL.md</code>的例子：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">---</span>
name: web-scraping
description: 使用scpr命令行工具，根据提供的网址抓取网页内容。
<span class="nt">---</span>

当需要抓取网页时，请按以下方式使用scpr命令行工具:

<span class="sb">```</span>bash
scpr <span class="nt">--url</span> &lt;https://example.com&gt;
</code></pre></div></div>

<p>当然了你也可以将mcp，甚至是其他的Skills封装到Skills中，比如</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">---</span>
name: web-scraping
description: 使用scpr命令行工具，根据提供的网址抓取网页内容。
<span class="nt">---</span>

<span class="c">## 概述</span>
本技能让 AI 能够通过 <span class="k">**</span>MCP（模型上下文协议）<span class="k">**</span> 调用 <span class="sb">`</span>scpr<span class="sb">`</span> 命令行工具，实现对公开网页内容的抓取。
所有工具定义、参数解析、错误处理和执行流程，均由 MCP 服务端统一管理。
AI 只需关注<span class="k">**</span>何时触发<span class="k">**</span>和<span class="k">**</span>如何处理用户输入<span class="k">**</span>，无需编写任何胶水代码。

<span class="c">## 技能用途</span>
- 从用户提供的 URL 中抓取网页文本、元数据和主要内容
- 使用标准化的 MCP 工具调用，替代零散的自定义函数调用
- 由 MCP 自动管理超时、异常和 CLI 执行细节

<span class="c">## 触发条件</span>
- 用户明确提出“抓取、爬取、提取”某个网页内容的需求
- 用户提供了具体的网页 URL，并希望获取该页面的信息

<span class="c">## MCP 集成（由 MCP 服务端预定义）</span>
MCP 服务端会<span class="k">**</span>自动暴露<span class="k">**</span>以下工具，无需手动定义函数、参数解析或错误处理。

<span class="c">### MCP 工具名称</span>
<span class="sb">`</span>web_scrape<span class="sb">`</span>

<span class="c">### MCP 自动提供的参数</span>
- <span class="sb">`</span>url<span class="sb">`</span> <span class="o">(</span>字符串，必填<span class="o">)</span>：需要抓取的网页 URL

<span class="c">### MCP 自动处理的功能</span>
- 参数校验
- CLI 命令执行：<span class="sb">`</span>scpr scrape <span class="nt">--url</span> &lt;url&gt;<span class="sb">`</span>
- 网络超时
- 无效 URL 拦截
- 连接失败处理
- 统一返回格式

<span class="c">## 技能行为（给 AI 的执行指令）</span>
当用户请求抓取网页时：
1. 从用户提问中提取出目标 <span class="k">**</span>URL<span class="k">**</span>
2. 通过 MCP 调用 <span class="sb">`</span>web_scrape<span class="sb">`</span> 工具，并传入该 URL
3. 将抓取到的内容整理为清晰易读的格式回复给用户
4. 如果 MCP 返回错误，将其以友好的方式告知用户

<span class="c">## 执行流程示例</span>
<span class="c">### 用户输入</span>
帮我抓取这个网页：shturl.cc/bbhhySzVq

<span class="c">### AI 通过 MCP 执行的动作</span>
调用 MCP 工具（内部格式）：
<span class="sb">```</span>json
<span class="o">{</span>
  <span class="s2">"toolName"</span>: <span class="s2">"web_scrape"</span>,
  <span class="s2">"arguments"</span>: <span class="o">{</span>
    <span class="s2">"url"</span>: <span class="s2">"shturl.cc/bbhhySzVq"</span>
  <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="三-function-callmcpskill三者总结">三. function call、MCP、SKILL三者总结</h3>

<ul>
  <li>
    <p><strong>Function Call（全手动）</strong>：构建管道，自定义工具，</p>
  </li>
  <li>
    <p><strong>MCP（全自动）</strong>，构建管道，连接标准化</p>
  </li>
  <li>
    <p><strong>Skills（老司机手册）</strong>：流程结构化，AI 会做复杂任务</p>
  </li>
</ul>]]></content><author><name>李小肥</name><email>yeyansiwangtt@gmail.com</email></author><category term="NLP" /><category term="DeepLearning" /><summary type="html"><![CDATA[大模型的工具对比]]></summary></entry><entry><title type="html">大模型的强化学习微调</title><link href="https://lixiaofei2yy.website/2026/03/14/%E5%A4%A7%E6%A8%A1%E5%9E%8B%E7%9A%84%E5%BC%BA%E5%8C%96%E5%AD%A6%E4%B9%A0%E5%BE%AE%E8%B0%83.html" rel="alternate" type="text/html" title="大模型的强化学习微调" /><published>2026-03-14T08:00:00+08:00</published><updated>2026-03-14T08:00:00+08:00</updated><id>https://lixiaofei2yy.website/2026/03/14/%E5%A4%A7%E6%A8%A1%E5%9E%8B%E7%9A%84%E5%BC%BA%E5%8C%96%E5%AD%A6%E4%B9%A0%E5%BE%AE%E8%B0%83</id><content type="html" xml:base="https://lixiaofei2yy.website/2026/03/14/%E5%A4%A7%E6%A8%A1%E5%9E%8B%E7%9A%84%E5%BC%BA%E5%8C%96%E5%AD%A6%E4%B9%A0%E5%BE%AE%E8%B0%83.html"><![CDATA[<h3 id="一-rt强化学习">一. RT强化学习</h3>

<h4 id="11-训练过程的演进">1.1 训练过程的演进</h4>

<ul>
  <li>第一阶段：self-supervised Learning，这里我们可以理解为基座大模型，输入：人工智，输出：慧</li>
  <li>第二阶段：supervised Learning，这里就是SFT，输入：你是谁？输出：我</li>
  <li>第三阶段：Reinforcement Learning, 强化学习，输入：世界上最高的山是？输出：喜马拉雅山（而不是输出 我不知道）</li>
</ul>

<h4 id="12-什么是rft">1.2 什么是RFT</h4>

<p>RFT：reinforcement fine-tuning是强化学习微调。就是在已经预训练好的大模型基础上，<strong>用 “奖励机制” 再训练一遍</strong>，让模型<strong>越来越会做你想要它做的事</strong>。</p>

<ul>
  <li>Fine-tuning：微调，在已经训练好的模型上，用少量数据再训练，让模型更贴合你的任务</li>
  <li>Reinforcement：强化学习，给模型回答的结果进行打分，进行<strong>奖励/惩罚</strong></li>
</ul>

<p>这里使用<strong>李宏毅老师</strong>的讲解RF的ppt，我们就能很直观的了解RF是什么了</p>

<p><img src="https://raw.githubusercontent.com/yy2lyx/picgo/admin/img/lhy_1.png" alt="" /></p>

<p><img src="https://raw.githubusercontent.com/yy2lyx/picgo/admin/img/lhy_2.png" alt="" /></p>

<h4 id="13-为什么llm需要rft">1.3 为什么LLM需要RFT</h4>

<p>对于LLM来说，什么样的输出才能算 “好文本”呢：它不仅仅关乎语法或事实，还关乎人类的品味、思路的连贯性、推理的正确性、消除输出中不必要的偏见等等。</p>

<p>因此我们需要强化学习来帮忙LLM来适应 <strong>符合人类的偏好</strong>：</p>

<ul>
  <li><strong>人类来评判LLM，而非公式：</strong>人类成为我们的“奖励函数”。</li>
  <li><strong>学习人类的喜好：</strong>我们训练 LLM 生成人类更可能喜欢的文本。</li>
</ul>

<h4 id="14-rft和sft的对比">1.4 RFT和SFT的对比</h4>

<table>
  <thead>
    <tr>
      <th> </th>
      <th>RFT</th>
      <th>SFT</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>训练量级</td>
      <td>少于100</td>
      <td>10w以上</td>
    </tr>
    <tr>
      <td>训练时间</td>
      <td>更快</td>
      <td>漫长</td>
    </tr>
    <tr>
      <td>模型学习</td>
      <td>基于正反case进行迭代</td>
      <td>对于输入和输出的关联进行记忆</td>
    </tr>
    <tr>
      <td>类比</td>
      <td>教会了AI如何去思考+行为</td>
      <td>用信息数据填充AI的脑子里面</td>
    </tr>
  </tbody>
</table>

<h4 id="15-什么场景适用于rft">1.5 什么场景适用于RFT</h4>

<ul>
  <li>没有标签数据：可以通过自己定义的验证代码来判别输出结果的正确性</li>
  <li>只有少量的标签数据：没有足够的量级支持SFT</li>
  <li>需要COT来提升模型表现：RFT能让COT的过程控制变短，从而提升模型推理速度</li>
  <li>需要多个步骤的逻辑推理：当需要模型需要一步一步进行决策的时候，而不是简单的直接映射关系，RFT更适合</li>
  <li>更快速的进行模型的迭代：以更快的速度，更短时间进行模型的实时迭代</li>
</ul>

<h4 id="16-function-call中rf的应用">1.6 Function Call中RF的应用</h4>

<p>如果大家在项目中经常使用function call或者需要将大模型输出的结果进行二次解析的话，经常就会遇到一个很蛋疼的问题，那就是 明明提示词写的就是让LLM输出 指定的json结构的返回结果，但是往往 LLM 最终输出的都是非结构化的数据。</p>

<p>因此我们可以利用RF对模型进行训练，专门强化输出结构化的推理结果。</p>

<h3 id="二-rt的演进">二. RT的演进</h3>

<h4 id="21-奖励模型">2.1 奖励模型</h4>

<p>为什么需要奖励模型？</p>

<p>我们不能让人类在LLM的训练过程中对每个次训练迭代的输出结果进行评判，那样会太慢了，因此我们需要训练一个奖励模型——<strong>一个学习模仿人类偏好的AI评判模型</strong>：</p>

<ul>
  <li><strong>奖励模型 = AI 口味测试员：</strong>我们使用人类偏好数据对其进行训练。它会学习给人类倾向于喜欢的文本赋予更高的分数。</li>
  <li><strong>强化学习算法使用奖励模型：</strong>诸如 PPO、DPO 和 GRPO 之类的算法使用这种奖励模型来指导语言学习模型 (LLM) 的学习。LLM 尝试生成能够获得 AI 评委高分的文本。</li>
</ul>

<h4 id="22-ppo">2.2 PPO</h4>

<blockquote>
  <p>PPO论文：https://arxiv.org/abs/1707.06347</p>

  <p>InstructGPT论文：https://arxiv.org/abs/2203.02155</p>
</blockquote>

<p>这个是OpenAI在17年提出的<strong>近端策略优化 (PPO)</strong>，到22年OpenAI提出了<strong>InstructGPT</strong>之后，将 PPO 提升到了一个全新的高度。这篇论文展示了如何利用 PPO 根据<strong>人类偏好</strong>来微调大规模语言模型（例如 GPT-3）。InstructGPT 不再仅仅是预测下一个词，而是<strong>学会了生成人类真正喜欢的文本</strong>——既有帮助又无害的文本。</p>

<p>以下是PPO训练的步骤：</p>

<ul>
  <li>生成文本：LLM通过不同提示词来生成推理的文本</li>
  <li>奖励模型对生成的文本进行评分。</li>
  <li>利用广义优势估计 (GAE)来计算优势：它会考虑多个词的奖励，平衡方差（MC）和偏差（TD）之间的权衡。这就像不仅在最后给予奖励，而且在过程中也会对表现良好的“小步骤”给予奖励，能计算每个词选择的提升幅度。</li>
  <li>KL散度的惩罚：新旧结果的差异很大，会施加惩罚，从而增强稳定性。</li>
  <li>更新价值函数：训练价值函数，使其准确地预测不同文本生成的“好坏”。</li>
</ul>

<h4 id="23-dpo">2.3 DPO</h4>

<blockquote>
  <p>DPO论文地址：https://arxiv.org/abs/2305.18290</p>
</blockquote>

<p>这是斯坦福大学在24年7月发布的直接偏好优化 Direct Preference Optimization(DPO)：</p>

<ul>
  <li>直接：DPO 就像直接告诉 LLM：“ A 比 B 好。多像 A，少像 B！”它省去了策略更新步骤中的中间环节（强化学习中用于策略优化的奖励模型）。</li>
  <li>不需要迭代循环：利用<strong>类似分类的损失——直接使用logits</strong>损失函数，该函数直接比较两个模型的概率 ，直接基于人类偏好数据优化 LLM</li>
</ul>

<p><img src="https://raw.githubusercontent.com/yy2lyx/picgo/admin/img/dpo.png" alt="" /></p>

<h4 id="24-grpo">2.4 GRPO</h4>

<blockquote>
  <p>GRPO论文：https://arxiv.org/pdf/2402.03300</p>
</blockquote>

<p>这个是DeepSeek在24年8月发布的强化学习的方式：Group Relative Policy Optimization（GRPO）。GRPO基于PPO，旨在简化RLHF训练流程，使其速度更快，尤其适用于复杂的推理任务。</p>

<ul>
  <li>去掉了PPO的Value Model：更加轻量高效</li>
  <li>核心Group Relative Policy Optimization：不再通过分组分数来估算基线，从而显著减少训练资源消耗。使用一组由 LLM 生成的针对同一提示的回答，来评估每个回答相对于组内其他回答的“优劣” 。</li>
</ul>

<p><img src="https://raw.githubusercontent.com/yy2lyx/picgo/admin/img/grpo.png" alt="" /></p>

<h3 id="三-基于gsm8k数据集的grpo的实操">三. 基于GSM8K数据集的GRPO的实操</h3>

<h4 id="31-数据准备">3.1 数据准备</h4>

<p>这里用推荐的GSM8K数学问答的数据集</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">datasets</span> <span class="kn">import</span> <span class="n">load_dataset</span>

<span class="k">def</span> <span class="nf">extract_hash_answer</span><span class="p">(</span><span class="n">text</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">Extract numerical answer from GSM8K format (#### marker)</span><span class="sh">"""</span>
    <span class="k">if</span> <span class="sh">"</span><span class="s">####</span><span class="sh">"</span> <span class="ow">not</span> <span class="ow">in</span> <span class="n">text</span><span class="p">:</span>
        <span class="k">return</span> <span class="bp">None</span>
    <span class="k">return</span> <span class="n">text</span><span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="sh">"</span><span class="s">####</span><span class="sh">"</span><span class="p">)[</span><span class="mi">1</span><span class="p">].</span><span class="nf">strip</span><span class="p">()</span>


<span class="k">def</span> <span class="nf">process_dataset_example</span><span class="p">(</span><span class="n">example</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">Convert GSM8K example to conversation format for GRPO training</span><span class="sh">"""</span>
    <span class="n">question</span> <span class="o">=</span> <span class="n">example</span><span class="p">[</span><span class="sh">"</span><span class="s">question</span><span class="sh">"</span><span class="p">]</span>
    <span class="n">answer</span> <span class="o">=</span> <span class="nf">extract_hash_answer</span><span class="p">(</span><span class="n">example</span><span class="p">[</span><span class="sh">"</span><span class="s">answer</span><span class="sh">"</span><span class="p">])</span>
    <span class="n">prompt</span> <span class="o">=</span> <span class="p">[</span>
        <span class="p">{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">system</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="n">system_prompt</span><span class="p">},</span>
        <span class="p">{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="n">question</span><span class="p">},</span>
    <span class="p">]</span>

    <span class="k">return</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">prompt</span><span class="sh">"</span><span class="p">:</span> <span class="n">prompt</span><span class="p">,</span>  <span class="c1"># Input conversation
</span>        <span class="sh">"</span><span class="s">answer</span><span class="sh">"</span><span class="p">:</span> <span class="n">answer</span><span class="p">,</span>  <span class="c1"># Ground truth for reward functions
</span>    <span class="p">}</span>


<span class="k">def</span> <span class="nf">load</span><span class="p">():</span>
    <span class="n">dataset</span> <span class="o">=</span> <span class="nf">load_dataset</span><span class="p">(</span><span class="sh">"</span><span class="s">openai/gsm8k</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">main</span><span class="sh">"</span><span class="p">,</span> <span class="n">split</span><span class="o">=</span><span class="sh">"</span><span class="s">train</span><span class="sh">"</span><span class="p">)</span>
    <span class="c1"># Apply conversation formatting to all examples
</span>    <span class="n">dataset</span> <span class="o">=</span> <span class="n">dataset</span><span class="p">.</span><span class="nf">map</span><span class="p">(</span><span class="n">process_dataset_example</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">dataset</span>
</code></pre></div></div>

<h4 id="32-定义奖励机制">3.2 定义奖励机制</h4>

<p>实施四种互补的奖励函数来评估数学推理的不同方面：</p>

<ol>
  <li><strong>格式完全匹配</strong>：结构完全一致</li>
  <li><strong>近似匹配</strong>：格式元素部分得分</li>
  <li><strong>答案正确性</strong>：数学准确性，分级评分</li>
  <li><strong>数字提取</strong>：解析和输出数值结果的能力</li>
</ol>

<p>这里举其中第一个例子</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">match_format_exactly</span><span class="p">(</span><span class="n">completions</span><span class="p">,</span> <span class="o">**</span><span class="n">kwargs</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    完美遵守格式可获得高额奖励（3.0）
    确保模型学习完整的结构化输出模式
    </span><span class="sh">"""</span>
    <span class="n">scores</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">completion</span> <span class="ow">in</span> <span class="n">completions</span><span class="p">:</span>
        <span class="n">response</span> <span class="o">=</span> <span class="n">completion</span><span class="p">[</span><span class="mi">0</span><span class="p">][</span><span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">]</span>
        <span class="c1"># Check if response matches complete format pattern
</span>        <span class="n">score</span> <span class="o">=</span> <span class="mf">3.0</span> <span class="k">if</span> <span class="n">match_format</span><span class="p">.</span><span class="nf">search</span><span class="p">(</span><span class="n">response</span><span class="p">)</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span> <span class="k">else</span> <span class="mf">0.0</span>
        <span class="n">scores</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">score</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">scores</span>
</code></pre></div></div>

<h4 id="33-训练">3.3 训练</h4>

<p>训练过程其实和sft很类似，都是基于lora进行训练，但是唯一差别就是定义了reward function</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Configure GRPO training parameters for mathematical reasoning
</span><span class="n">training_args</span> <span class="o">=</span> <span class="nc">GRPOConfig</span><span class="p">(</span>
    <span class="n">learning_rate</span><span class="o">=</span><span class="mf">5e-6</span><span class="p">,</span>
    <span class="n">per_device_train_batch_size</span><span class="o">=</span><span class="mi">2</span><span class="p">,</span>
    <span class="n">gradient_accumulation_steps</span><span class="o">=</span><span class="mi">8</span><span class="p">,</span>
    <span class="n">max_prompt_length</span><span class="o">=</span><span class="mi">1024</span><span class="p">,</span> 
    <span class="n">max_completion_length</span><span class="o">=</span><span class="mi">1024</span><span class="p">,</span>
    <span class="n">max_steps</span><span class="o">=</span><span class="mi">10</span><span class="p">,</span> 
    <span class="n">logging_steps</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> 
    <span class="n">output_dir</span><span class="o">=</span><span class="sh">"</span><span class="s">./trl_grpo_outputs</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">max_grad_norm</span><span class="o">=</span><span class="mf">0.1</span><span class="p">,</span>
<span class="p">)</span>

<span class="n">trainer</span> <span class="o">=</span> <span class="nc">GRPOTrainer</span><span class="p">(</span>
    <span class="n">model</span><span class="o">=</span><span class="n">model</span><span class="p">,</span>      
    <span class="n">reward_funcs</span><span class="o">=</span><span class="p">[</span>                   
        <span class="n">match_format_exactly</span><span class="p">,</span>       
    <span class="p">],</span>
    <span class="n">args</span><span class="o">=</span><span class="n">training_args</span><span class="p">,</span>               
    <span class="n">train_dataset</span><span class="o">=</span><span class="n">dataset</span><span class="p">,</span>            
<span class="p">)</span>
</code></pre></div></div>
<p>至此，训练的过程就结束啦！</p>]]></content><author><name>李小肥</name><email>yeyansiwangtt@gmail.com</email></author><category term="NLP" /><category term="DeepLearning" /><summary type="html"><![CDATA[大模型的强化学习类型]]></summary></entry><entry><title type="html">基于Ollama本地搭建OpenClaw私人助手</title><link href="https://lixiaofei2yy.website/2026/03/05/%E5%9F%BA%E4%BA%8EOllama%E6%9C%AC%E5%9C%B0%E6%90%AD%E5%BB%BAOpenClaw%E7%A7%81%E4%BA%BA%E5%8A%A9%E6%89%8B.html" rel="alternate" type="text/html" title="基于Ollama本地搭建OpenClaw私人助手" /><published>2026-03-05T08:00:00+08:00</published><updated>2026-03-05T08:00:00+08:00</updated><id>https://lixiaofei2yy.website/2026/03/05/%E5%9F%BA%E4%BA%8EOllama%E6%9C%AC%E5%9C%B0%E6%90%AD%E5%BB%BAOpenClaw%E7%A7%81%E4%BA%BA%E5%8A%A9%E6%89%8B</id><content type="html" xml:base="https://lixiaofei2yy.website/2026/03/05/%E5%9F%BA%E4%BA%8EOllama%E6%9C%AC%E5%9C%B0%E6%90%AD%E5%BB%BAOpenClaw%E7%A7%81%E4%BA%BA%E5%8A%A9%E6%89%8B.html"><![CDATA[<blockquote>
  <p>官网：https://openclaw.ai/，目前已经从clawdbot更名为moltbot了，现在又更名为openclaw了。</p>
</blockquote>

<h3 id="一-openclaw为什么那么火爆">一. OpenClaw为什么那么火爆</h3>

<p>OpenClaw（原 Clawdbot/Moltbot）能快速爆火，核心是精准击中本地隐私、全场景自动化、低门槛使用三大刚需，叠加开源社区与社交传播助力，形成现象级增长。GitHub 短时间星标破 10 万，一周网站访问量超 200 万，成为 2026 年初增长最快的开源 AI 项目之一。下面则是爆火的原因：</p>

<ul>
  <li>隐私可控：所有数据完全存储与本地，而且支持本地大模型，隐私更加可控。</li>
  <li>任务自动化：基于AI agent，可自动处理邮件、日程、文件、代码、网页自动化。</li>
  <li>多渠道接入：支持Telegram、WhatsApp、Discord、iMessage 等应用，但是微信目前还不支持。</li>
</ul>

<h3 id="二-mac上本地安装">二. mac上本地安装</h3>

<h4 id="21-下载ollama本地模型">2.1 下载ollama本地模型</h4>

<blockquote>
  <p>ollama官网下载ollama注意需要下载最新版本，下载必要的本地/cloud模型</p>

  <p>参考ollama的文档：https://docs.ollama.com/integrations/openclaw</p>
</blockquote>

<p>这里使用<code class="language-plaintext highlighter-rouge">ollama run --verbose qwen3-coder:latest</code>来测试模型的生成速度：</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">total duration</code>：总耗时</li>
  <li><code class="language-plaintext highlighter-rouge">load duration</code>：模型加载时间</li>
  <li><code class="language-plaintext highlighter-rouge">eval rate</code>：推理生成tokens</li>
</ul>

<p>下载推荐的llm，下载模型：<code class="language-plaintext highlighter-rouge">ollama pull llm</code>，下面测试是基于Macbook pro的M4芯片测试的结果：</p>

<table>
  <thead>
    <tr>
      <th>模型</th>
      <th>大小</th>
      <th>token的速度</th>
      <th>简介</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>qwen3-coder</td>
      <td>18G</td>
      <td>68.37 tokens/s</td>
      <td><strong>千问团队于2025年7月推出的新一代开源AI编程大模型系列，专为代码生成、智能代理（Agent）和仓库级编程优化</strong></td>
    </tr>
    <tr>
      <td>glm-4.7-flash</td>
      <td>19G</td>
      <td>48.96 tokens/s</td>
      <td>智谱AI于2025年12月发布的GLM-4系列大模型，<strong>主打高吞吐量和高性价比，特别强化了本地编程和Agent任务能力</strong></td>
    </tr>
    <tr>
      <td>gpt-oss:20b</td>
      <td>13G</td>
      <td>51.01 tokens/s</td>
      <td><strong>OpenAI于2025年8月发布的一款200亿参数的开源权重混合专家模型</strong>它专为轻量级推理和工具调用场景设计，性能接近o3-mini，能在16GB显存的消费级硬件或边缘设备上高效运行，具备极高的本地部署性价比</td>
    </tr>
  </tbody>
</table>

<h4 id="22-搭建openclaw环境">2.2 搭建OpenClaw环境</h4>

<blockquote>
  <p>参考ollama的openclaw的博客：https://ollama.com/blog/openclaw</p>
</blockquote>

<ul>
  <li>
    <p>构建conda环境：<code class="language-plaintext highlighter-rouge">conda create -n clawdbot python=3.13 -y</code></p>
  </li>
  <li>
    <p>激活环境：<code class="language-plaintext highlighter-rouge">conda activate clawdbot</code></p>
  </li>
  <li>
    <p>下载<a href="https://nodejs.org/en/download">nodes</a>：</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Download and install nvm:</span>
curl <span class="nt">-o-</span> https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash
  
<span class="c"># in lieu of restarting the shell</span>
<span class="se">\.</span> <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.nvm/nvm.sh"</span>
  
<span class="c"># Download and install Node.js:</span>
nvm <span class="nb">install </span>24
  
<span class="c"># Verify the Node.js version:</span>
node <span class="nt">-v</span> <span class="c"># Should print "v24.13.0".</span>
  
<span class="c"># Verify npm version:</span>
npm <span class="nt">-v</span> <span class="c"># Should print "11.6.2".</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>安装/更新open claw：<code class="language-plaintext highlighter-rouge">npm install -g openclaw@latest</code></p>
  </li>
  <li>
    <p>安装线上wizard：<code class="language-plaintext highlighter-rouge">openclaw onboard --install-daemon</code></p>
  </li>
  <li>
    <p>从ollama启动openclaw：<code class="language-plaintext highlighter-rouge">ollama launch openclaw</code></p>
  </li>
  <li>
    <p>将node，npm，openclaw设置环境变量：<code class="language-plaintext highlighter-rouge">sudo vim ~/.zshrc</code></p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">PATH</span><span class="o">=</span>~/.nvm/versions/node/v24.13.0/bin:<span class="nv">$PATH</span>
<span class="nb">export </span><span class="nv">PATH</span><span class="o">=</span><span class="s2">"</span><span class="si">$(</span>npm prefix <span class="nt">-g</span><span class="si">)</span><span class="s2">/bin:</span><span class="nv">$PATH</span><span class="s2">"</span>
</code></pre></div>    </div>
  </li>
</ul>

<p>本地快速查看openclaw，聊天/查看配置：<code class="language-plaintext highlighter-rouge">http://127.0.0.1:18789/chat</code></p>

<h4 id="23-让openclaw爬取互联网信息">2.3 让openclaw爬取互联网信息</h4>

<ol>
  <li>
    <p>安装chromium（这里可以理解为开源的google chrome浏览器）：</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># 安装 Chromium
brew install --cask chromium
   
# 移除隔离属性（首次打开可能需要）
xattr -dr com.apple.quarantine /Applications/Chromium.app
</code></pre></div>    </div>
  </li>
  <li>
    <p>更改openclaw的json文件，其目录在<code class="language-plaintext highlighter-rouge">~/.openclaw/openclaw.json</code>，或者直接通过打开本地的dashboard（<code class="language-plaintext highlighter-rouge">openclaw dashboard</code>）来查看和更改config。</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"browser":{
    "enabled":true,
    "executablePath":"/Applications/Chromium.app/Contents/MacOS/Chromium",
    "headless":false,
    "noSandbox":true,
    "attachOnly":false,
    "defaultProfile":"openclaw"
  }
</code></pre></div>    </div>
  </li>
  <li>
    <p>重启openclaw的网关：<code class="language-plaintext highlighter-rouge">openclaw gateway restart</code></p>
  </li>
  <li>
    <p>查看browser的状态：<code class="language-plaintext highlighter-rouge">openclaw browser status</code></p>
  </li>
  <li>
    <p>启动专属的浏览器：<code class="language-plaintext highlighter-rouge">openclaw browser start</code></p>
  </li>
  <li>
    <p>修改web配置：<code class="language-plaintext highlighter-rouge">openclaw configure --section web</code></p>
  </li>
</ol>

<h3 id="三-open-claw配置agent-skills">三. open claw配置agent skills</h3>

<blockquote>
  <p>clawhub地址：https://docs.openclaw.ai/tools/clawhub#clawhub</p>
</blockquote>

<h4 id="31-agent-skills的优势">3.1 agent skills的优势</h4>

<p>相较于没有skills的open claw，有agent skills的优势：</p>

<ul>
  <li>相同的workflow，每次使用，不需要重复去解释</li>
  <li>持久化：对话的数据不会持久化，而skill的数据会在本地存储</li>
  <li>节约tokens：没有skill每次需要重复的对话占用token</li>
</ul>

<h4 id="32-skills-安装和使用">3.2 skills 安装和使用</h4>

<blockquote>
  <p>Clawhub 官网：https://clawhub.ai</p>
</blockquote>

<p>这里推荐使用的是我们直接clawhub的官网搜索需要的skills，找到对应的名字，比如我这里找到的就是浏览器的agent skills——Agent Browser，它可以帮助我们实现自动爬虫的功能（模拟人浏览页面的操作），我们在安装的时候也能发现，安装这个skill，需要安装playwright这个框架。</p>

<p><img src="https://raw.githubusercontent.com/yy2lyx/picgo/admin/img/clawhub.png" alt="" /></p>

<ol>
  <li>安装clawhub插件：<code class="language-plaintext highlighter-rouge">npm i -g clawhub</code></li>
  <li>安装agent-browser：<code class="language-plaintext highlighter-rouge">clawhub install "agent-browser"</code></li>
</ol>

<p>如果上面第二步下载不成功，也可以基于git来安装：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>npm <span class="nb">install</span> <span class="nt">-g</span> pnpm
git clone https://github.com/vercel-labs/agent-browser
<span class="nb">cd </span>agent-browser
pnpm <span class="nb">install
</span>pnpm build
agent-browser <span class="nb">install</span>
</code></pre></div></div>

<p>至此，就安装好了！</p>]]></content><author><name>李小肥</name><email>yeyansiwangtt@gmail.com</email></author><category term="NLP" /><category term="DeepLearning" /><summary type="html"><![CDATA[记录学习openclaw的过程]]></summary></entry><entry><title type="html">大模型基于检索增强的微调-RAFT</title><link href="https://lixiaofei2yy.website/2025/11/27/%E5%9F%BA%E4%BA%8Eunsloth%E6%9D%A5%E5%AE%9E%E7%8E%B0RAFT.html" rel="alternate" type="text/html" title="大模型基于检索增强的微调-RAFT" /><published>2025-11-27T08:00:00+08:00</published><updated>2025-11-27T08:00:00+08:00</updated><id>https://lixiaofei2yy.website/2025/11/27/%E5%9F%BA%E4%BA%8Eunsloth%E6%9D%A5%E5%AE%9E%E7%8E%B0RAFT</id><content type="html" xml:base="https://lixiaofei2yy.website/2025/11/27/%E5%9F%BA%E4%BA%8Eunsloth%E6%9D%A5%E5%AE%9E%E7%8E%B0RAFT.html"><![CDATA[<h3 id="一-raft简单介绍">一. RAFT简单介绍</h3>

<blockquote>
  <p>论文：<a href="https://arxiv.org/abs/2403.10131">《RAFT: Adapting Language Model to Domain Specific RAG》</a></p>
  <h4 id="11-raft的优势">1.1 RAFT的优势</h4>
  <p>RAFT其实是基于RAG（检索增强）的SFT（有监督微调），那么他和这两者的差异是什么呢？</p>
</blockquote>

<ol>
  <li>RAG：检索增强是基于提出的问题在向量数据库中检索相应的片段，然后作为线索合并问题喂给大模型，然后输出。然后这里会出现一个问题：<strong>模型对于特定领域的知识没有得到提前学习的机会，说白了就是给了大模型很多材料，但是无法非常有效的使用这些材料</strong>。</li>
  <li>SFT：有监督微调是基于提供特定的QA对的方式来喂给大模型，然后能够让大模型额外扩充知识面的方式。但是这也会存在一个问题：<strong>这些QA对更多的是让大模型增加了额外文档的记忆，而忽略了在回答实际问题时使用文档的机会，要么没有正确处理在寻找合适的文档来学习时出现的错误</strong>。</li>
  <li>RAFT：基于检索增强的微调则是结合RAG+SFT提出的一种新的微调方案。他的优势：
    <ul>
      <li>过微调确保模型在特定领域的知识上得到良好的记忆和训练，从而达到对不准确的检索文档进行发现。</li>
      <li>通过训练模型来将“理解问题”、“检索知识”和“正确答案”进行关联，从而提高达模型回答的准确性。</li>
    </ul>
  </li>
</ol>

<p>这也就是整片文章中一致强调的RAFT其实是一种“开卷考试” + “特定领域的提前学习”。
<img src="https://raw.githubusercontent.com/yy2lyx/picgo/admin/img/raft.png" alt="raft" /></p>

<h4 id="12-raft如何训练">1.2 RAFT如何训练</h4>
<p>区别于常规的SFT，RAFT在训练中加入了干扰的文档 + 正确的文档让模型来学习。<strong>这使得模型学习到的是基于记忆和文档的混合判断</strong>，参考论文中的“which is a mixture of both memorization and reading”。</p>

<ul>
  <li>训练的内容分为2块：
    <ul>
      <li>找到核心依据：QA对 + 标准答案的文档 + 多个误导的文档 + 思维链</li>
      <li>正确的例子：QA对 + 标准答案的文档 + 思维链</li>
    </ul>
  </li>
  <li>训练的核心：通过思维链来解释找到的答案，从而基于上下文信息，思考其答案，并链接到相关文档。</li>
</ul>

<p><img src="https://raw.githubusercontent.com/yy2lyx/picgo/admin/img/raft_train.png" alt="raft_train" /></p>
<h3 id="二-基于unsloth和lamma_index来实现faft">二. 基于unsloth和lamma_index来实现FAFT</h3>
<h4 id="21-基于ollama和lamma_index来构建数据集">2.1 基于ollama和lamma_index来构建数据集</h4>

<p>RAFT的数据集的特点是：</p>
<ul>
  <li>QA对</li>
  <li>正确文档</li>
  <li>错误文档</li>
  <li>思维链</li>
</ul>

<p>所以我们需要一个能够提供思维链的大模型，帮助我们生成上述这些内容。</p>
<ul>
  <li>模型：Qwen3-32B，选择这个千问模型是因为中文很友好+支持思维链+阿里的预训练的数据很顶</li>
  <li>调用框架：ollama，调用比如openai需要花钱，这个本地部署，调用不花钱！</li>
</ul>

<h4 id="22-安装相关依赖">2.2 安装相关依赖</h4>
<p>这里需要使用以下几个深度学习相关的框架：</p>
<ul>
  <li>lamma_index: 和langchain感觉很像，只是这个更加偏重于增强大型语言模型 (LLM) 处理广泛和异构数据集的能力。当然也能帮助我们构建raft数据集咯。</li>
  <li>unsloth: 大模型训练的框架</li>
</ul>

<p>依赖：</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>llama_index
llama-index-packs-raft-dataset
llama-index-llms-ollama
llama-index-embeddings-ollama
llama-index-packs-rag-evaluator
unsloth
</code></pre></div></div>
<h4 id="23-构建raft数据集">2.3 构建raft数据集</h4>
<p>我这边选择了法律相关的文书作为文档，然后结合lamma_index来向大模型提问，进而生成训练所需的问题和答案以及思维链。</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">os</span>
<span class="kn">import</span> <span class="n">datasets</span>
<span class="kn">import</span> <span class="n">pandas</span> <span class="k">as</span> <span class="n">pd</span>
<span class="kn">from</span> <span class="n">llama_index.packs.raft_dataset</span> <span class="kn">import</span> <span class="n">RAFTDatasetPack</span>
<span class="kn">from</span> <span class="n">llama_index.llms.ollama</span> <span class="kn">import</span> <span class="n">Ollama</span>
<span class="kn">from</span> <span class="n">llama_index.embeddings.ollama</span> <span class="kn">import</span> <span class="n">OllamaEmbedding</span>

<span class="k">def</span> <span class="nf">gen_qa_raft_dataset</span><span class="p">():</span>
    <span class="n">llm</span> <span class="o">=</span> <span class="nc">Ollama</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">qwen3:32b</span><span class="sh">"</span><span class="p">,</span> <span class="n">request_timeout</span><span class="o">=</span><span class="mf">60.0</span><span class="p">)</span>

    <span class="n">embed_model</span> <span class="o">=</span> <span class="nc">OllamaEmbedding</span><span class="p">(</span><span class="n">model_name</span><span class="o">=</span><span class="sh">"</span><span class="s">modelscope.cn/yingda/gte-Qwen2-1.5B-instruct-GGUF:latest</span><span class="sh">"</span><span class="p">)</span>

    <span class="n">sub_dir</span> <span class="o">=</span> <span class="sh">'</span><span class="s">data/laws/</span><span class="sh">'</span>

    <span class="n">df_list</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">file_path</span> <span class="ow">in</span> <span class="n">os</span><span class="p">.</span><span class="nf">listdir</span><span class="p">(</span><span class="n">sub_dir</span><span class="p">):</span>
        <span class="nf">print</span><span class="p">(</span><span class="n">file_path</span><span class="p">)</span>
        <span class="n">file_path</span> <span class="o">=</span> <span class="n">os</span><span class="p">.</span><span class="n">path</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">sub_dir</span><span class="p">,</span> <span class="n">file_path</span><span class="p">)</span>
        <span class="n">raft_dataset</span> <span class="o">=</span> <span class="nc">RAFTDatasetPack</span><span class="p">(</span><span class="n">file_path</span><span class="p">,</span>
                                       <span class="n">llm</span><span class="o">=</span><span class="n">llm</span><span class="p">,</span>
                                       <span class="n">embed_model</span><span class="o">=</span><span class="n">embed_model</span><span class="p">,</span>
                                       <span class="n">num_questions_per_chunk</span><span class="o">=</span><span class="mi">3</span><span class="p">,</span>
                                       <span class="n">num_distract_docs</span><span class="o">=</span><span class="mi">3</span><span class="p">,</span>
                                       <span class="n">chunk_size</span><span class="o">=</span><span class="mi">2048</span><span class="p">)</span>
        <span class="n">dataset</span> <span class="o">=</span> <span class="n">raft_dataset</span><span class="p">.</span><span class="nf">run</span><span class="p">()</span>
        <span class="n">df</span> <span class="o">=</span> <span class="n">dataset</span><span class="p">.</span><span class="nf">to_pandas</span><span class="p">()</span>
        <span class="n">df</span><span class="p">[</span><span class="sh">'</span><span class="s">messages</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="n">df</span><span class="p">.</span><span class="nf">apply</span><span class="p">(</span><span class="k">lambda</span> <span class="n">row</span><span class="p">:</span> <span class="p">[{</span><span class="sh">'</span><span class="s">content</span><span class="sh">'</span><span class="p">:</span> <span class="n">row</span><span class="p">[</span><span class="sh">'</span><span class="s">instruction</span><span class="sh">'</span><span class="p">],</span> <span class="sh">'</span><span class="s">role</span><span class="sh">'</span><span class="p">:</span> <span class="sh">'</span><span class="s">user</span><span class="sh">'</span><span class="p">},</span>
                                               <span class="p">{</span><span class="sh">'</span><span class="s">content</span><span class="sh">'</span><span class="p">:</span> <span class="n">row</span><span class="p">[</span><span class="sh">'</span><span class="s">cot_answer</span><span class="sh">'</span><span class="p">],</span> <span class="sh">'</span><span class="s">role</span><span class="sh">'</span><span class="p">:</span> <span class="sh">'</span><span class="s">assistant</span><span class="sh">'</span><span class="p">}],</span> <span class="n">axis</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
        <span class="n">df_list</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">df</span><span class="p">)</span>

    <span class="n">df_all</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="nf">concat</span><span class="p">(</span><span class="n">df_list</span><span class="p">)</span>
    <span class="n">dataset</span> <span class="o">=</span> <span class="n">datasets</span><span class="p">.</span><span class="n">Dataset</span><span class="p">.</span><span class="nf">from_pandas</span><span class="p">(</span><span class="n">df_all</span><span class="p">)</span>

    <span class="n">dataset</span><span class="p">.</span><span class="nf">to_csv</span><span class="p">(</span><span class="sh">"</span><span class="s">data/法律文书_raft_dataset.csv</span><span class="sh">"</span><span class="p">,</span> <span class="n">encoding</span><span class="o">=</span><span class="sh">'</span><span class="s">utf-8</span><span class="sh">'</span><span class="p">)</span>
</code></pre></div></div>

<p>然后我们可以打开看下结果，会发现包含了一下列：</p>
<ul>
  <li>question：大模型生成的问题</li>
  <li>context：多个文档（这里就是raft中的正确文档+误导文档）</li>
  <li>cot_answer: 思维链答案</li>
  <li>instruction: 多个文档 + 问题</li>
  <li>message: 完整的基于rag的问答对</li>
</ul>

<h4 id="24-基于lora的sft">2.4 基于lora的sft</h4>
<ol>
  <li>加载上述存储的dataset
    <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">df</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="nf">read_csv</span><span class="p">(</span><span class="sh">"</span><span class="s">data/法律文书_raft_dataset.csv</span><span class="sh">"</span><span class="p">,</span> <span class="n">index_col</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
<span class="n">dataset</span> <span class="o">=</span> <span class="n">datasets</span><span class="p">.</span><span class="n">Dataset</span><span class="p">.</span><span class="nf">from_pandas</span><span class="p">(</span><span class="n">df</span><span class="p">)</span>
</code></pre></div>    </div>
  </li>
  <li>构建dataset的template</li>
</ol>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">tokenizer</span><span class="p">.</span><span class="n">chat_template</span> <span class="o">=</span> <span class="n">DEFAULT_CHAT_TEMPLATE</span>
<span class="n">SYSTEM_PROMPT</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">你是一位很有能力的问题解答者，能够根据问题和相关背景提供答案。</span><span class="sh">"""</span>
<span class="k">def</span> <span class="nf">apply_chat_template</span><span class="p">(</span><span class="n">example</span><span class="p">,</span> <span class="n">tokenizer</span><span class="p">):</span>
    <span class="n">messages</span> <span class="o">=</span> <span class="n">example</span><span class="p">[</span><span class="sh">"</span><span class="s">messages</span><span class="sh">"</span><span class="p">]</span>
    <span class="n">messages</span> <span class="o">=</span> <span class="n">messages</span><span class="p">.</span><span class="nf">replace</span><span class="p">(</span><span class="sh">'</span><span class="se">\n</span><span class="sh">'</span><span class="p">,</span><span class="sh">'</span><span class="s">,</span><span class="sh">'</span><span class="p">)</span>
    <span class="n">messages</span> <span class="o">=</span> <span class="nf">eval</span><span class="p">(</span><span class="n">messages</span><span class="p">)</span>
    <span class="c1"># We add an empty system message if there is none
</span>    <span class="k">if</span> <span class="n">messages</span><span class="p">[</span><span class="mi">0</span><span class="p">][</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">]</span> <span class="o">!=</span> <span class="sh">"</span><span class="s">assistant</span><span class="sh">"</span><span class="p">:</span>
        <span class="n">messages</span><span class="p">.</span><span class="nf">insert</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="p">{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">system</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="n">SYSTEM_PROMPT</span><span class="p">})</span>
    <span class="n">example</span><span class="p">[</span><span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="n">tokenizer</span><span class="p">.</span><span class="nf">apply_chat_template</span><span class="p">(</span><span class="n">messages</span><span class="p">,</span> <span class="n">tokenize</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>

    <span class="k">return</span> <span class="n">example</span>
<span class="n">column_names</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="n">dataset</span><span class="p">.</span><span class="n">features</span><span class="p">)</span>
<span class="n">raw_datasets</span> <span class="o">=</span> <span class="n">dataset</span><span class="p">.</span><span class="nf">map</span><span class="p">(</span><span class="n">apply_chat_template</span><span class="p">,</span>
                                <span class="n">num_proc</span><span class="o">=</span><span class="nf">cpu_count</span><span class="p">(),</span>
                                <span class="n">fn_kwargs</span><span class="o">=</span><span class="p">{</span><span class="sh">"</span><span class="s">tokenizer</span><span class="sh">"</span><span class="p">:</span> <span class="n">tokenizer</span><span class="p">},</span>
                                <span class="n">remove_columns</span><span class="o">=</span><span class="n">column_names</span><span class="p">,</span>
                                <span class="n">desc</span><span class="o">=</span><span class="sh">"</span><span class="s">Applying chat template</span><span class="sh">"</span><span class="p">,)</span>
<span class="n">raw_datasets</span> <span class="o">=</span> <span class="n">raw_datasets</span><span class="p">.</span><span class="nf">train_test_split</span><span class="p">(</span><span class="n">test_size</span><span class="o">=</span><span class="mf">0.1</span><span class="p">)</span>
<span class="c1"># create the splits
</span><span class="n">train_dataset</span> <span class="o">=</span> <span class="n">raw_datasets</span><span class="p">[</span><span class="sh">"</span><span class="s">train</span><span class="sh">"</span><span class="p">]</span>
<span class="n">eval_dataset</span> <span class="o">=</span> <span class="n">raw_datasets</span><span class="p">[</span><span class="sh">"</span><span class="s">test</span><span class="sh">"</span><span class="p">]</span>
</code></pre></div></div>

<ol>
  <li>加载模型</li>
</ol>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 加载模型 - 使用Unsloth的FastLanguageModel加载预训练的Qwen3-8B模型
</span><span class="n">model</span><span class="p">,</span> <span class="n">tokenizer</span> <span class="o">=</span> <span class="n">FastLanguageModel</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span>
    <span class="n">model_name</span><span class="o">=</span><span class="sh">'</span><span class="s">Qwen/Qwen3-0.6B</span><span class="sh">'</span><span class="p">,</span>  <span class="c1"># 模型路径
</span>    <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="n">float16</span><span class="p">,</span>  <span class="c1"># 使用float16数据类型以减少内存占用
</span>    <span class="n">max_seq_length</span><span class="o">=</span><span class="mi">2048</span><span class="p">,</span>  <span class="c1"># 设置最大序列长度为2048
</span>    <span class="n">load_in_4bit</span><span class="o">=</span><span class="bp">False</span>  <span class="c1"># 使用4位量化加载模型，进一步减少显存占用
</span><span class="p">)</span>
<span class="n">model</span> <span class="o">=</span> <span class="n">FastLanguageModel</span><span class="p">.</span><span class="nf">get_peft_model</span><span class="p">(</span>
    <span class="n">model</span><span class="p">,</span>
    <span class="n">r</span> <span class="o">=</span> <span class="mi">32</span><span class="p">,</span>           <span class="c1"># Choose any number &gt; 0! Suggested 8, 16, 32, 64, 128
</span>    <span class="n">target_modules</span> <span class="o">=</span> <span class="p">[</span><span class="sh">"</span><span class="s">q_proj</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">k_proj</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">v_proj</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">o_proj</span><span class="sh">"</span><span class="p">,</span>
                      <span class="sh">"</span><span class="s">gate_proj</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">up_proj</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">down_proj</span><span class="sh">"</span><span class="p">,],</span>
    <span class="n">lora_alpha</span> <span class="o">=</span> <span class="mi">32</span><span class="p">,</span>  <span class="c1"># Best to choose alpha = rank or rank*2
</span>    <span class="n">lora_dropout</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="c1"># Supports any, but = 0 is optimized
</span>    <span class="n">bias</span> <span class="o">=</span> <span class="sh">"</span><span class="s">none</span><span class="sh">"</span><span class="p">,</span>    <span class="c1"># Supports any, but = "none" is optimized
</span>    <span class="c1"># [NEW] "unsloth" uses 30% less VRAM, fits 2x larger batch sizes!
</span>    <span class="n">use_gradient_checkpointing</span> <span class="o">=</span> <span class="sh">"</span><span class="s">unsloth</span><span class="sh">"</span><span class="p">,</span> <span class="c1"># True or "unsloth" for very long context
</span>    <span class="n">random_state</span> <span class="o">=</span> <span class="mi">3407</span><span class="p">,</span>
    <span class="n">use_rslora</span> <span class="o">=</span> <span class="bp">False</span><span class="p">,</span>   <span class="c1"># We support rank stabilized LoRA
</span>    <span class="n">loftq_config</span> <span class="o">=</span> <span class="bp">None</span><span class="p">,</span>  <span class="c1"># And LoftQ
</span><span class="p">)</span>
</code></pre></div></div>

<ol>
  <li>训练</li>
</ol>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">trainer</span> <span class="o">=</span> <span class="nc">SFTTrainer</span><span class="p">(</span>
    <span class="n">model</span> <span class="o">=</span> <span class="n">model</span><span class="p">,</span>
    <span class="n">train_dataset</span> <span class="o">=</span> <span class="n">train_dataset</span><span class="p">,</span>
    <span class="n">eval_dataset</span> <span class="o">=</span> <span class="n">eval_dataset</span><span class="p">,</span>
    <span class="n">args</span> <span class="o">=</span> <span class="nc">SFTConfig</span><span class="p">(</span>
        <span class="n">dataset_text_field</span> <span class="o">=</span> <span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">per_device_train_batch_size</span> <span class="o">=</span> <span class="mi">2</span><span class="p">,</span>
        <span class="n">gradient_accumulation_steps</span> <span class="o">=</span> <span class="mi">4</span><span class="p">,</span> <span class="c1"># Use GA to mimic batch size!
</span>        <span class="n">warmup_steps</span> <span class="o">=</span> <span class="mi">5</span><span class="p">,</span>
        <span class="c1"># num_train_epochs = 1, # Set this for 1 full training run.
</span>        <span class="n">max_steps</span> <span class="o">=</span> <span class="mi">30</span><span class="p">,</span>
        <span class="n">learning_rate</span> <span class="o">=</span> <span class="mf">2e-4</span><span class="p">,</span> <span class="c1"># Reduce to 2e-5 for long training runs
</span>        <span class="n">logging_steps</span> <span class="o">=</span> <span class="mi">1</span><span class="p">,</span>
        <span class="n">optim</span> <span class="o">=</span> <span class="sh">"</span><span class="s">adamw_8bit</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">weight_decay</span> <span class="o">=</span> <span class="mf">0.01</span><span class="p">,</span>
        <span class="n">lr_scheduler_type</span> <span class="o">=</span> <span class="sh">"</span><span class="s">linear</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">seed</span> <span class="o">=</span> <span class="mi">3407</span><span class="p">,</span>
        <span class="n">report_to</span> <span class="o">=</span> <span class="sh">"</span><span class="s">none</span><span class="sh">"</span><span class="p">,</span> <span class="c1"># Use this for WandB etc
</span>    <span class="p">),</span>
<span class="p">)</span>
<span class="n">tokenizer</span><span class="p">.</span><span class="n">pad_token</span> <span class="o">=</span> <span class="n">tokenizer</span><span class="p">.</span><span class="n">eos_token</span>
<span class="n">train_result</span> <span class="o">=</span> <span class="n">trainer</span><span class="p">.</span><span class="nf">train</span><span class="p">()</span>
</code></pre></div></div>

<h3 id="三-raft的总结">三. RAFT的总结</h3>
<p>RAFT和一般的SFT而言，其实本质上没有任何差别，只是说将RAG融合进了有监督微调中，从而达到一种让模型拥有更加准确筛选文档+使用特定领域类文档的能力。而在训练中增加误导的文档，更是能让模型达到一种思考和过滤无关信息的能力。</p>]]></content><author><name>李小肥</name><email>yeyansiwangtt@gmail.com</email></author><category term="NLP" /><category term="DeepLearning" /><summary type="html"><![CDATA[记录学习大模型的RAFT]]></summary></entry><entry><title type="html">大模型的检索增强-NanoGraphRAG</title><link href="https://lixiaofei2yy.website/2025/06/09/graphrag.html" rel="alternate" type="text/html" title="大模型的检索增强-NanoGraphRAG" /><published>2025-06-09T08:00:00+08:00</published><updated>2025-06-09T08:00:00+08:00</updated><id>https://lixiaofei2yy.website/2025/06/09/graphrag</id><content type="html" xml:base="https://lixiaofei2yy.website/2025/06/09/graphrag.html"><![CDATA[<h3 id="一-rag和graphrag">一. RAG和GraphRAG</h3>

<h4 id="11-什么是rag">1.1 什么是RAG</h4>
<blockquote>
  <p>RAG（Retrieval Augmented Generation，检索增强生成）是一种将信息检索技术与生成式语言模型（LLM）结合的AI框架。它通过从外部知识库中检索相关信息，然后使用这些信息增强LLM的提示词（prompt），从而生成更准确、更相关、更全面的答案。﻿</p>
</blockquote>

<p>大模型为什么需要RAG：</p>
<ul>
  <li>如果一个llm在pretrain之后没有做rag（外部检索）的话，其实往往可能存在幻觉。</li>
  <li>基座模型在专业领域知识不足，比如一些医药、法律等。</li>
  <li>外挂知识需要长期实时维护更新的。</li>
</ul>

<h4 id="12-rag在llm中langchain的例子">1.2 RAG在LLM中LangChain的例子</h4>
<p><img src="https://raw.githubusercontent.com/yy2lyx/picgo/admin/img/rag_lanchain.jpg" alt="LangChain" />
如上图所示，整个RAG的过程分为三个部分：</p>
<ul>
  <li>文本嵌入阶段：将本地文本进行分段后，利用embedding模型对其每个段落进行词嵌入，并将其vector存入向量数据库（比如FAISS、Chroma）中。</li>
  <li>请求阶段：对大模型进行请求之前，会将请求的文本同样利用embedding模型转成向量，然后会和向量数据库中比对相似度最高的本地文本段落，并将其作为关联的文本段放到prompt中。</li>
  <li>输出阶段：将提示词 + 本地文本关联段落 + 请求一起输入大模型，得到输出。</li>
</ul>

<h4 id="13-graphrag的定义及其优势">1.3 GraphRAG的定义及其优势</h4>
<blockquote>
  <p>GraphRAG是一种基于知识图谱的检索增强技术，通过构建图模型的知识表达，将实体和关系之间的联系用图的形式进行展示，然后利用大语言模型LLM进行检索增强。
论文：<a href="https://arxiv.org/abs/2404.16130">From Local to Global: A Graph RAG Approach to Query-Focused Summarization</a></p>
</blockquote>

<p>GraphRAG比RAG优势在哪：</p>
<ul>
  <li>RAG过于局限：RAG仅能检索到与query相速度最高的文本片段，无法从全局出发来看整个本地文本。</li>
  <li>GraphRAG通过构建实体关系图谱实现了信息间的连接，能从全局出发，能更完整地理解和检索复杂的关联信息，从而生成更准确和全局性的结果。</li>
</ul>

<h4 id="14-graphrag的流程">1.4 GraphRAG的流程</h4>
<p><img src="https://raw.githubusercontent.com/yy2lyx/picgo/admin/img/graphrag.png" alt="LangChain" />
如上图所示，就是GraphRAG的处理流程图，其实总结起来就两个部分：
第一部分：构建知识图谱</p>
<ul>
  <li>将本地文本进行分段</li>
  <li>对每个段落进行摘要提取：这里完全依赖于<strong>大模型的提取能力</strong></li>
  <li>实体和关系的提取：这里完全依赖于<strong>大模型的提取能力</strong>，实体和关系抽取可以参考本人之前写的《事件抽取实战》这篇文章</li>
  <li>构建知识图谱，写入数据库</li>
</ul>

<p>第二部分：利用构建的知识图谱回答问题</p>
<ul>
  <li>社区检测：实际就是一种图聚类的方法，这里的“社区”，我们可以将其理解为一类具有相同特性的节点的集合。</li>
  <li>社区总结：给每个社区创建类似报告的摘要。利于理解整体的结构和语义。</li>
  <li>生成全局答案：给定用户查询，基于上一步生成的社区摘要可用于在多阶段过程中生成最终答案。</li>
</ul>

<h3 id="二-graphrag的轻量版本nano-graphrag">二. GraphRAG的轻量版本——nano-GraphRAG</h3>
<p>对于GraphRAG而言，其封装的太死，代码量又很大，对于定制化的任务而言，不太好下手，因此推荐一个轻量化的版本——nano-GraphRAG。</p>

<blockquote>
  <p>代码：https://github.com/gusye1234/nano-graphrag</p>
</blockquote>

<p>相较于GraphRAG的优势：</p>
<ul>
  <li>代码量少：包含了tests + prompt也才只有1100行代码！</li>
  <li>更容易阅读</li>
  <li>给的例子太好用了，直接上手没有难度。</li>
</ul>

<h4 id="21-安装">2.1 安装</h4>
<p>这里推荐不用官方推荐的pip install 来安装。因为这样会对于代码和prompt不好调整。
直接clone项目即可，然后安装其依赖<code class="language-plaintext highlighter-rouge">pip install -r requirements.txt</code></p>

<h4 id="22-基于ollama的nano-graphrag的三国例子">2.2 基于ollama的nano-GraphRAG的三国例子</h4>
<p>由于nano-Graphrag中的例子已经包含ollama形式的调用大模型的方式，因此开箱即用即可：</p>
<ol>
  <li>找到<code class="language-plaintext highlighter-rouge">examples/using_ollama_as_llm_and_embedding.py</code>，这里需要你更改embedding大模型和chat大模型，我这里选择了魔塔社区的embedding模型，注意由于我们使用的是ollama来本地启动大模型，因此需要选择的是GGUF格式的模型。
    <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">MODEL</span> <span class="o">=</span> <span class="sh">"</span><span class="s">deepseek-r1:8b</span><span class="sh">"</span>
<span class="n">EMBEDDING_MODEL</span> <span class="o">=</span> <span class="sh">"</span><span class="s">modelscope.cn/yingda/gte-Qwen2-1.5B-instruct-GGUF:latest</span><span class="sh">"</span>
<span class="n">EMBEDDING_MODEL_DIM</span> <span class="o">=</span> <span class="mi">1536</span>
</code></pre></div>    </div>
  </li>
  <li>配置工程目录：
    <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">WORKING_DIR</span> <span class="o">=</span> <span class="sh">"</span><span class="s">/LLM/nano-graphrag/three_kingdom_ds</span><span class="sh">"</span>
</code></pre></div>    </div>
  </li>
  <li>修改insert的文件地址：</li>
</ol>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">with</span> <span class="nf">open</span><span class="p">(</span><span class="sh">"</span><span class="s">/LLM/nano-graphrag/three_kingdom_ds/三国演义.txt</span><span class="sh">"</span><span class="p">,</span> <span class="n">encoding</span><span class="o">=</span><span class="sh">"</span><span class="s">utf-8-sig</span><span class="sh">"</span><span class="p">)</span> <span class="k">as</span> <span class="n">f</span><span class="p">:</span>
</code></pre></div></div>
<ol>
  <li>使用ollama开启本地模型
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ollama run deepseek-r1:8b
ollama run modelscope.cn/yingda/gte-Qwen2-1.5B-instruct-GGUF:latest
</code></pre></div>    </div>
  </li>
</ol>

<p>然后直接<code class="language-plaintext highlighter-rouge">python examples/using_ollama_as_llm_and_embedding.py</code>就会自动构建知识图谱 + 查询了。</p>

<h4 id="23-爬坑">2.3 爬坑</h4>
<p>之前本人已经实验过<code class="language-plaintext highlighter-rouge">qwen3:8b</code>,亲测会报错<code class="language-plaintext highlighter-rouge">leiden.EmptyNetworkError: EmptyNetworkError</code></p>

<p>然后解决方案有2种：</p>
<ul>
  <li>更换大模型，比如像上面使用<code class="language-plaintext highlighter-rouge">deepseek-r1:8b</code>或者<code class="language-plaintext highlighter-rouge">mistral:7b</code>都可以的</li>
  <li>更改prompt</li>
</ul>

<p>然后推荐测试的话，可以不用上来就用《三国演义》，不然可能跑了好几个小时，发现出现问题，那真的搞心态啊！</p>]]></content><author><name>李小肥</name><email>yeyansiwangtt@gmail.com</email></author><category term="NLP" /><category term="DeepLearning" /><summary type="html"><![CDATA[记录学习大模型的RAG]]></summary></entry><entry><title type="html">大模型的微调和推理加速</title><link href="https://lixiaofei2yy.website/2025/05/27/%E5%9F%BA%E4%BA%8E%E5%A4%A7%E6%A8%A1%E5%9E%8B%E7%9A%84%E5%BE%AE%E8%B0%83%E5%92%8C%E6%8E%A8%E7%90%86%E5%8A%A0%E9%80%9F.html" rel="alternate" type="text/html" title="大模型的微调和推理加速" /><published>2025-05-27T08:00:00+08:00</published><updated>2025-05-27T08:00:00+08:00</updated><id>https://lixiaofei2yy.website/2025/05/27/%E5%9F%BA%E4%BA%8E%E5%A4%A7%E6%A8%A1%E5%9E%8B%E7%9A%84%E5%BE%AE%E8%B0%83%E5%92%8C%E6%8E%A8%E7%90%86%E5%8A%A0%E9%80%9F</id><content type="html" xml:base="https://lixiaofei2yy.website/2025/05/27/%E5%9F%BA%E4%BA%8E%E5%A4%A7%E6%A8%A1%E5%9E%8B%E7%9A%84%E5%BE%AE%E8%B0%83%E5%92%8C%E6%8E%A8%E7%90%86%E5%8A%A0%E9%80%9F.html"><![CDATA[<h3 id="一-大模型微调">一. 大模型微调</h3>
<h4 id="11-微调的原因">1.1 微调的原因</h4>
<p>在业务中直接使用大模型，往往会发现：</p>
<ul>
  <li>模型对prompt非常敏感，需要不断调整，迭代prompt的过程很痛苦</li>
  <li>本身能力不满足独有的业务需求</li>
  <li>模型的更新/更换 导致相同的prompt却拿到不同的结果</li>
</ul>

<h4 id="12-微调的方式">1.2 微调的方式</h4>
<ul>
  <li>zero-shot prompting（0样本）：撰写prompt，迭代prompt，获取答案。无需数据，无需额外开发、部署成本。</li>
  <li>few-shot prompting（1-10样本）：收集样例，迭代样例，获取答案。需要提供少量的样本（正反例）。</li>
  <li>adaptation（1k-1w样本）：收集数据，适配模型，部署模型。
    <ul>
      <li>适用百亿参数以下的模型</li>
      <li>少量的适配特殊场景/任务的样本，这里其实可以利用更大参数、更优的大模型输出的结果来训练这类“小模型”</li>
    </ul>
  </li>
  <li>更深层次的微调：
    <ul>
      <li>领域预训练：用无监督领域数据继续训练基座模型</li>
      <li>全参数精调：用有监督领域数据精调基座模型</li>
      <li>检索增强：外挂知识库，这里比如一般的RAG，还有比较火热的GraphRAG。</li>
      <li>多智能体：多模型/agent共同完成任务。</li>
    </ul>
  </li>
</ul>

<h3 id="二-llm微调工具-unsloth">二. LLM微调工具-Unsloth</h3>
<blockquote>
  <p>git地址：https://github.com/unslothai/unsloth</p>
</blockquote>

<p>选择unsloth作为微调工具的理由：</p>
<ul>
  <li>训练更快</li>
  <li>显存占用更少</li>
  <li>迭代快，模型适配广，目前都能支持Qwen3了</li>
</ul>

<h4 id="21-依赖的安装">2.1 依赖的安装</h4>
<blockquote>
  <p>这里爬了不少坑，因此记录下</p>
</blockquote>

<ol>
  <li>直接pip安装：<code class="language-plaintext highlighter-rouge">pip install unsloth</code>, 这里会默认安装最新的torch版本，而非适配于自身系统cuda版本的torch（用<code class="language-plaintext highlighter-rouge">nvcc -V</code>可查看cuda版本）</li>
  <li>重新安装torch，本人这里cuda是12.1的：<code class="language-plaintext highlighter-rouge">pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu121</code></li>
  <li>重新安装对应cuda版本的xformers：<code class="language-plaintext highlighter-rouge">pip install -U xformers --index-url https://download.pytorch.org/whl/cu121</code></li>
  <li>如果需要微调Qwen3，那么对于transformers有版本要求：<code class="language-plaintext highlighter-rouge">pip install transformers==4.51.0 --no-deps</code></li>
  <li>改了transformers之后，那么也需要对应的trl版本更改：<code class="language-plaintext highlighter-rouge">pip install trl==0.17.0 --no-deps</code></li>
  <li>安装vllm:<code class="language-plaintext highlighter-rouge">pip install vllm==0.7 --no-deps</code></li>
  <li>这里会遇到unsloth和vllm版本不匹配的问题：<code class="language-plaintext highlighter-rouge">No module named vllm.lora.peft_helper</code>,简单的处理方案就是到<code class="language-plaintext highlighter-rouge">site-packages/unsloth_zoo/vllm_lora_worker_manager.py</code>文件中注释掉所有的和<code class="language-plaintext highlighter-rouge">PEFTHelper</code>相关的行即可。</li>
  <li>如果出现<code class="language-plaintext highlighter-rouge">No platform detected, vLLM is running on UnspecifiedPlatform</code>，这里只需要安装<code class="language-plaintext highlighter-rouge">pip install  pynvml==12.0</code>即可。</li>
</ol>

<h4 id="22-微调的例子">2.2 微调的例子</h4>
<p>这里推荐使用官方的基于Qwen3的微调例子：https://colab.research.google.com/github/unslothai/notebooks/blob/main/nb/Qwen3_(14B)-Reasoning-Conversational.ipynb</p>

<p>当然在上述微调的例子中，也存了坑，就在训练模型过程中：</p>
<ul>
  <li>对于高版本的trl而言，参数从<code class="language-plaintext highlighter-rouge">tokenizer</code>变成了<code class="language-plaintext highlighter-rouge">processing_class</code></li>
  <li>如果不指定data_collator的话，会出现padding相关的bug。</li>
</ul>

<p>下面的代码是修复之后的</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 配置训练参数 - 使用SFTTrainer进行监督微调
</span><span class="n">trainer</span> <span class="o">=</span> <span class="nc">SFTTrainer</span><span class="p">(</span>
    <span class="n">model</span><span class="o">=</span><span class="n">model</span><span class="p">,</span>  <span class="c1"># 要训练的模型
</span>    <span class="n">processing_class</span><span class="o">=</span><span class="n">tokenizer</span><span class="p">,</span>  <span class="c1"># 分词器
</span>    <span class="n">train_dataset</span><span class="o">=</span><span class="n">combined_dataset</span><span class="p">,</span>  <span class="c1"># 训练数据集
</span>    <span class="n">data_collator</span><span class="o">=</span><span class="nc">DataCollatorForLanguageModeling</span><span class="p">(</span><span class="n">tokenizer</span><span class="p">,</span><span class="n">mlm</span><span class="o">=</span><span class="bp">False</span><span class="p">),</span> <span class="c1"># 定义data_collator，一定是这个不然会报错
</span>    <span class="c1"># max_seq_length=1024,  # 训练时使用的最大序列长度
</span>    <span class="n">args</span><span class="o">=</span><span class="nc">SFTConfig</span><span class="p">(</span>  <span class="c1"># SFT配置参数
</span>        <span class="n">dataset_num_proc</span> <span class="o">=</span> <span class="mi">12</span><span class="p">,</span>
        <span class="n">dataset_text_field</span><span class="o">=</span><span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">,</span>  <span class="c1"># 数据集中文本字段的名称
</span>        <span class="n">per_device_train_batch_size</span><span class="o">=</span><span class="mi">2</span><span class="p">,</span>  <span class="c1"># 每个设备的训练批次大小
</span>        <span class="n">gradient_accumulation_steps</span><span class="o">=</span><span class="mi">4</span><span class="p">,</span>  <span class="c1"># 梯度累积步数，用于模拟更大的批次大小
</span>        <span class="n">warmup_steps</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span>  <span class="c1"># 学习率预热步数
</span>        <span class="c1"># num_train_epochs = 2,  # 训练轮数，这里注释掉了，使用max_steps代替
</span>        <span class="n">max_steps</span><span class="o">=</span><span class="mi">50</span><span class="p">,</span>  <span class="c1"># 最大训练步数
</span>        <span class="n">max_seq_length</span><span class="o">=</span><span class="mi">1024</span><span class="p">,</span>
        <span class="n">learning_rate</span><span class="o">=</span><span class="mf">2e-4</span><span class="p">,</span>  <span class="c1"># 学习率，对于长时间训练可以降低到2e-5
</span>        <span class="n">logging_steps</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span>  <span class="c1"># 日志记录间隔步数
</span>        <span class="n">optim</span><span class="o">=</span><span class="sh">"</span><span class="s">adamw_8bit</span><span class="sh">"</span><span class="p">,</span>  <span class="c1"># 优化器，使用8位AdamW
</span>        <span class="n">weight_decay</span><span class="o">=</span><span class="mf">0.01</span><span class="p">,</span>  <span class="c1"># 权重衰减率
</span>        <span class="n">lr_scheduler_type</span><span class="o">=</span><span class="sh">"</span><span class="s">linear</span><span class="sh">"</span><span class="p">,</span>  <span class="c1"># 学习率调度器类型
</span>        <span class="n">seed</span><span class="o">=</span><span class="mi">3407</span><span class="p">,</span>  <span class="c1"># 随机种子
</span>        <span class="n">report_to</span><span class="o">=</span><span class="sh">"</span><span class="s">none</span><span class="sh">"</span><span class="p">,</span>  <span class="c1"># 报告工具，可以设置为"wandb"等
</span>    <span class="p">),</span>
<span class="p">)</span>

</code></pre></div></div>

<h3 id="三-推理加速">三. 推理加速</h3>
<p>对于训练完成后的模型，我们需要选择一个合适的加速推理的框架来加速大模型的推理。</p>

<h4 id="31-常用框架">3.1 常用框架</h4>
<p>常用的推理加速框架：</p>
<ul>
  <li>LLM-tensorRT：在TensorRT推理引擎基础上，针对大模型的推理优化框架</li>
  <li>vllm：通过PagedAttention高效地管理attention中缓存的张量</li>
  <li>LLMdeploy：LMDeploy 开发了 Persistent Batch(即 Continuous Batch)，Blocked K/V Cache，动态拆分和融合，张量并行，高效的计算 kernel等重要特性。推理性能是 vLLM 的 1.8 倍</li>
  <li>llama.cpp：针对C/C++的加速推理框架</li>
</ul>

<h4 id="32-vllm的使用">3.2 vLLM的使用</h4>
<p>对于企业级的<strong>服务以及高并发</strong>场景，vLLM会更加适合。</p>

<p>安装：<code class="language-plaintext highlighter-rouge">pip install vllm --extra-index-url https://download.pytorch.org/whl/cu121</code></p>

<p>以下是两种构建服务的方式：</p>
<ul>
  <li>第一种：使用<code class="language-plaintext highlighter-rouge">vllm.entrypoints.openai.api_server</code>，适用curl调用</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python <span class="nt">-m</span> vllm.entrypoints.openai.api_server <span class="nt">--model</span> <span class="s2">"模型地址"</span> <span class="nt">--served-model-name</span> <span class="s2">"模型名字"</span> <span class="nt">--port</span> 8123 <span class="nt">--dtype</span><span class="o">=</span>half
</code></pre></div></div>

<ul>
  <li>第二种：使用<code class="language-plaintext highlighter-rouge">vllm serve</code>，适用于openai的方式调用apikey</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vllm serve <span class="s2">"模型地址"</span> <span class="nt">--tensor-parallel-size</span> 1 <span class="nt">--enforce-eager</span> <span class="nt">--port</span> 8124 <span class="nt">--dtype</span> float16 <span class="nt">--trust-remote-code</span> <span class="nt">--served-model-name</span> 模型名字
</code></pre></div></div>
<p>然后可以使用openai来调用vllm启动模型的接口</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">openai</span> <span class="kn">import</span> <span class="n">OpenAI</span>
<span class="c1"># Set OpenAI's API key and API base to use vLLM's API server.
</span><span class="n">openai_api_key</span> <span class="o">=</span> <span class="sh">"</span><span class="s">EMPTY</span><span class="sh">"</span>
<span class="n">openai_api_base</span> <span class="o">=</span> <span class="sh">"</span><span class="s">http://localhost:8123/v1</span><span class="sh">"</span>

<span class="n">client</span> <span class="o">=</span> <span class="nc">OpenAI</span><span class="p">(</span>
    <span class="n">api_key</span><span class="o">=</span><span class="n">openai_api_key</span><span class="p">,</span>
    <span class="n">base_url</span><span class="o">=</span><span class="n">openai_api_base</span><span class="p">,</span>
<span class="p">)</span>

<span class="n">chat_response</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">chat</span><span class="p">.</span><span class="n">completions</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span>
    <span class="c1"># model="Qwen/Qwen3-8B",
</span>    <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">模型名字</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">messages</span><span class="o">=</span><span class="p">[</span>
        <span class="p">{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Give me a short introduction to large language models.</span><span class="sh">"</span><span class="p">},</span>
    <span class="p">],</span>
    <span class="c1"># max_tokens=32768,
</span>    <span class="n">temperature</span><span class="o">=</span><span class="mf">0.6</span><span class="p">,</span>
    <span class="n">top_p</span><span class="o">=</span><span class="mf">0.95</span><span class="p">,</span>
    <span class="n">extra_body</span><span class="o">=</span><span class="p">{</span>
        <span class="sh">"</span><span class="s">top_k</span><span class="sh">"</span><span class="p">:</span> <span class="mi">20</span><span class="p">,</span>
    <span class="p">},</span>
<span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Chat response:</span><span class="sh">"</span><span class="p">,</span> <span class="n">chat_response</span><span class="p">)</span>
</code></pre></div></div>]]></content><author><name>李小肥</name><email>yeyansiwangtt@gmail.com</email></author><category term="NLP" /><category term="DeepLearning" /><summary type="html"><![CDATA[记录学习大模型的微调和推理加速]]></summary></entry></feed>